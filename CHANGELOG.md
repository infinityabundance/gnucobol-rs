<!--
DO NOT EDIT BY HAND. Generated by xtask docs from docs-src/changelog.model.json
+ machine evidence (crates/gnucobol-rs/Cargo.toml, reports/claim-ladder.json, reports/casefiles/*,
reports/receipts/*). Regenerate: cargo run -p xtask -- docs generate. Check: cargo run -p xtask -- docs check.
Evidence authority: the claim-ladder + generated casefiles. Legacy source preserved (lossless) under research/legacyreports/CHANGELOG.md (sha256 512ea99caa74cbb9957bf182dc8611bb4174edb346b8c8dbae28a285eee54d35).
-->

# Changelog

> _Generated document (TRUST.4.DOCS). Machine authority: `reports/claim-ladder.json` + `reports/casefiles/`. Legacy source preserved losslessly under `research/legacyreports/CHANGELOG.md`._

All notable changes to `gnucobol-rs` are documented here. The project follows the
oracle-first method: each entry names the slice sealed and the parity it proved.

## [0.7.71]
- **`GNURUST.COMMON.NUMCHECK.1` sealed -- the not-numeric runtime diagnostic, verified against TWO oracles.** Faithful pure ports of common.c's `explain_field_type` (the field-type name map: `NUMERIC DISPLAY` / `PACKED-DECIMAL` / `COMP-6` / `ALPHANUMERIC` / ...) and `cob_check_numeric`, reproducing the EXACT EC-DATA-INCOMPATIBLE runtime error GnuCOBOL prints when a non-numeric value reaches arithmetic (under `cobc -debug`): `'<name>' (Type: <type>) not numeric: '<escaped>'`, where the escaping shows printable bytes as-is and non-printable bytes as `\<ooo>` octal (text fields) or the whole value as `0x<hex>` (otherwise). **Proven byte-identical across BOTH oracles 3.1.2 + 3.2** (`numeric_check_sweep` 2/0). The message is separated from the abort; `cob_is_numeric` (the validity decision) is taken as input and is a follow-on. Kani `numeric_check_is_conditional` + fuzz. common.c 209 -> 207 missing.

## [0.7.70]
- **`GNURUST.COMMON.BOUNDCHECK.1` sealed -- runtime bounds-check diagnostics, verified against TWO oracles.** Faithful pure ports of common.c's `cob_check_subscript` / `cob_check_odo` / `cob_check_ref_mod` / `cob_check_ref_mod_detailed` / `cob_check_ref_mod_minimal`, reproducing the EXACT `cob_runtime_error` + `cob_runtime_hint` text GnuCOBOL prints on an out-of-bounds table subscript, reference modification, or OCCURS DEPENDING ON length (under `cobc -debug`) -- e.g. `subscript of 'E' out of bounds: 5` + `maximum subscript for 'E': 3`. **A second oracle (GnuCOBOL 3.1.2) was built and the messages proven byte-identical across BOTH 3.1.2 and 3.2** (`bounds_check_sweep` 6/0) -- a version-stable claim, the first court verified differentially against two admitted oracles. The pure bounds decision + message text is separated from the abort side effect. Kani `subscript_check_is_exact` + fuzz. common.c 222 -> 209 missing. Non-claims: the `libcob:` prefix framing, the `cob_check_numeric` not-numeric message, and the abort/exit.

## [0.7.69]
- **common.c harvest -- 5 more pure helpers ported (common.c 222 -> 217 missing).** Faithful pure ports of `version_bitstring` (the `0xMMmmpp00` version packing), `cob_is_upper` / `cob_is_lower` (the all-space-or-ASCII-case class tests), `cob_int_to_string` (`sprintf("%i")`), and `cob_int_to_formatted_bytestring` (the binary-1024 `B`/`kB`/`MB` size formatter, `%3.2f`). Each is a side-effect-free function of its inputs, with unit tests against the C semantics. Continues mining the dependency-free subset of common.c (the runtime-mutator bulk still awaits its state/side-effect map).

## [0.7.68]
- **`GNURUST.SCREENIO.ATTR.1` sealed -- native SCREEN SECTION monochrome display attributes.** `DISPLAY ... HIGHLIGHT / LOWLIGHT / UNDERLINE / BLINK / REVERSE-VIDEO` now reproduces GnuCOBOL's terminal bytes exactly (`screenio_attr_sweep` 10/0, no ncurses linked). Each attribute wraps the field text in the SGR-on opener `\e(B\e[0;<n>m\e[39;49m\e[37m\e[40m` (charset + `set_attributes` + default-colour restore) and the constant SGR-off closer `\e(B\e[m\e[39;49m\e[37m\e[40m`, with `<n>` = 1/2/4/5/7. Because the SGR moves nothing, the attribute composes exactly with the sealed mvcur positioning. `screenio.rs` `ScreenAttr` + `sgr_on`/`sgr_off` + Kani `attribute_envelope` + fuzz. Non-claims: COLOUR attributes (FOREGROUND/BACKGROUND-COLOR trigger a whole-screen repaint -- a follow-on court), combined attributes, ACCEPT-side attributes, and any terminal but the admitted `TERM=xterm` / ncurses 6.6.

## [0.7.67]
- **`GNURUST.SCREENIO.DISPLAY.3` sealed -- the GENERAL ncurses mvcur (inter-field moves), multi-field DISPLAY native.** Generalizes the cursor model from a single from-home move to ANY move whose origin is not home, so a multi-field `DISPLAY` reproduces GnuCOBOL's terminal bytes for the inter-field moves (`screenio_multi_sweep` 21/0, no ncurses linked). `screenio.rs` `mvcur` now enumerates the strategies ncurses weighs -- keep-column-and-move-vertically (VPA / `cuu1`) then horizontally, carriage-return-then-vertical-then-horizontal, `home \e[H`, and direct CUP -- and emits the shortest with local/CR strategies winning a tie over CUP. The same function subsumes the from-home first-field move (DISPLAY.2, still 70/0) and the pause-prompt move (which follows the LAST field, not the lowest). Kani `multi_field_envelope` + fuzz. Honestly carved out: overlapping same-row layouts (a later field LEFT of an earlier one) trigger ncurses's REFRESH LINE-DIFF (`clr_eol \e[K` erase via the curses `TransformLine` algorithm) -- a separate mechanism declared as the next court, not claimed here. Attributes, ACCEPT, and any terminal but the admitted `TERM=xterm` / ncurses 6.6 remain non-claims.

## [0.7.66]
- **`GNURUST.SCREENIO.DISPLAY.2` sealed -- the ncurses mvcur cursor-cost model, reproduced natively.** Extends the screenio campaign from one fixed position to ANY single-field DISPLAY position, proven byte-identical across a LINE x COLUMN grid (`screenio_grid_sweep` 70/0, no ncurses linked). The cost model was reverse-engineered from the oracle and reproduced exactly: same-row column advance uses space-fill for <=4 cols, column-address HPA `\e[<c>G` for cols 6..8, and direct CUP `\e[<r>;<c>H` for col >=9; a row change uses VPA `\e[<r>d` + space-fill when the column is <=3, else CUP; and the post-field move to the pause prompt drops the row then backspaces (`\e[<r>d \x08`) for a column-1 field, else carriage-returns then drops the row. `screenio.rs` `move_cursor_from_home` + Kani `mvcur_is_bounded` + fuzz. Non-claims: positions outside the swept grid, multi-field (non-home inter-field moves), attributes, ACCEPT, and any terminal but the admitted `TERM=xterm` / ncurses 6.6.

## [0.7.65]
- **`GNURUST.SCREENIO.INIT.1` sealed -- native SCREEN SECTION terminal bytes, no ncurses; the screenio.c campaign begins.** GnuCOBOL drives ncurses for SCREEN SECTION I/O, and the literal terminal bytes are ncurses's terminfo-optimized output. Because that stream is *deterministic* for a fixed terminal, a pure-Rust emitter can reproduce it exactly -- proven BYTE-IDENTICAL against the oracle captured under a pty (`screenio_sweep` 1/0, no ncurses linked). The canonical `DISPLAY "X" LINE 2 COLUMN 3.` then `STOP RUN.` yields the same 230 bytes: the smcup / scroll-region / charset / default-color **init prologue**, the positioned field (VPA `\e[2d` + space-fill to column 3 + the byte), the libcob "end of program, please press a key to exit " pause, and the rmcup **teardown epilogue** -- to the byte. `screenio.rs` (`INIT_PROLOGUE`/`TEARDOWN_EPILOGUE`/`PAUSE_PROMPT`/`move_cursor`/`display_and_stop`) + Kani `display_framing_envelope` + fuzz `screenio`. The scope is honest and narrow: the admitted terminal (`TERM=xterm`, ncurses 6.6), one positioned literal/FROM field. The ncurses `mvcur` cursor-cost model (CUP vs VPA vs space-fill), color/attribute SGR, multi-field layout, numeric-edited display, and `ACCEPT` input are explicit non-claims + follow-on courts -- and the byte stream is terminfo-dependent, so it does not transfer to other terminals. This answers the screenio dependency question the same way as mlio: a faithful *native reproduction*, oracle-verified, not an ncurses binding.

## [0.7.64]
- **`GNURUST.MLIO.PARSE.1` sealed -- native XML PARSE, no libxml2; this completes `mlio.c` to 100%.** A from-scratch Rust port of the XML PARSE state machine (`cob_xml_parse`/`xml_parse`/`xml_free_parse_memory` over the modelled XML special registers `MlModule`/`XmlState` + `set_xml_event`/`set_xml_text`/`set_xml_code`/`get_xml_code`/`set_xml_exception`). The fidelity insight: **GnuCOBOL 3.2's XML PARSE is itself unimplemented** -- even built `WITH_XML2`, `mlio.c:xml_parse` advances `JUST_STARTED -> HAD_END_OF_INPUT` directly (the `xmlParseChunk` and "not implemented" branches are unreachable for an in-memory document), so a PROCESSING PROCEDURE observes exactly `START-OF-DOCUMENT -> END-OF-INPUT -> END-OF-DOCUMENT`, all with `XML-CODE 0` and (in the default XMLNSS mode) empty `XML-TEXT`. `ml_parse_sweep` 2/0 confirms the native state machine emits that identical sequence. Writing a *real* parser would DIVERGE from the authority, so faithfulness IS this state machine. Kani `xml_parse_terminates` (the loop always halts) + fuzz `mlio_parse`. Also ported the remaining GENERATE entry layer (`cob_xml_generate`/`xml_generate`/`cob_json_generate` with the `COUNT IN` field-write + too-small exception, oracle-checked by `ml_generate_sweep` COUNT rows) and the numeric-edit/hex-attribute/register helpers (`get_pic_for_num_field`/`get_num`/`generate_hex_attribute`/`xmlCharStrndup_void`/`get_trimmed_json_data`). Corrected `XML_INVALID_CHAR_REPLACED` to its true enum value `417` (was a `415` transcription slip). **mlio.c 66.0% -> 100%**, the 11th of 13 libcob files complete, with NO libxml2/cJSON linked anywhere.

## [0.7.63]
- **`GNURUST.MLIO.GENERATE.1` sealed -- native XML / JSON GENERATE, byte-identical to GnuCOBOL WITHOUT libxml2 / cJSON.** A from-scratch Rust serializer of the `cob_ml_tree` (`generate_xml_from_tree`/`generate_element`/`generate_normal_element`/`generate_hex_element`/`generate_content`/`generate_attributes`/`generate_normal_attribute` + `generate_json_from_tree` + `get_xml_name`/`get_xml_num`/`get_json_num` + `cob_xml_generate_new`/`cob_json_generate_new`), proven to produce the SAME bytes as the admitted GnuCOBOL's `XML GENERATE` / `JSON GENERATE` (which drive libxml2's `xmlTextWriter` + json-c) -- with no C library linked. `ml_generate_sweep` 2/0 against a cobc record with a signed numeric, a scaled numeric, an alphanumeric needing XML escaping, and a nested group: `<G><NEG>-42</NEG><DEC>12.50</DEC><SPC>a&lt;b&amp;c</SPC>...` and `{"G":{"NEG":-42,"DEC":12.50,"SPC":"a<b&c",...}}`. Numeric formatting strips leading zeros + inserts the decimal point at the scale; XML escapes `&`/`<`/`>`; JSON strings are quoted + escaped, numbers unquoted. Kani `xml_generate_total` + fuzz. This is the right answer to the dependency question: a faithful *native reimplementation*, oracle-verified, not a libxml2 binding. **mlio.c 27.7% -> 66.0%** (XML/JSON PARSE stays the declared libxml2/json-c boundary). The 7 functions the port-index briefly mis-counted as doc-only were a `b'"'` quote-char-literal tripping its tokenizer (written `0x22` now).
- **`common.c` + `mlio.c` -- the dependency-free subsets ported (the honest, no-new-dependency increment).** Per the rule "port what's possible WITHOUT pulling in a C library; otherwise map common.c first," mlio.c (XML/JSON GENERATE) and screenio.c (the Screen Section) are blocked from 100% by their external libraries -- mlio.c's serializers use **libxml2** (`xmlTextWriter`) and **cJSON**, screenio.c uses **ncurses** -- so those serializer/curses functions are the declared external-library boundary. **`common.c` 1.6% -> 7.1%** (+14): the *pure, side-effect-free* runtime primitives only (the panel's state/side-effect/exception/signal/config surface stays unported pending its map): the byte-comparison primitives behind `IF a = b` / `IF a = ALL "x"` (`compare_character`/`compare_spaces`/`compare_zeroes`/`common_cmpc`/`common_cmps`/`cob_cmp_all`/`cob_cmp_alnum`), the SORT key comparison (`sort_compare`/`sort_compare_collate`), the EBCDIC overpunch sign (`cob_put_sign_ebcdic`: neg `0-9`->`}JKLMNOPQR`, pos->`{ABCDEFGHI`), integer formatting (`ss_itoa_u10`), the ISO weekday shift (`one_indexed_day_of_week_from_monday`), and the boolean env parsing (`cob_check_env_true`/`cob_check_env_false`). **`mlio.c` 17% -> 27.7%** (+5): the dependency-free GENERATE helpers (`int_to_hex`, `copy_data_as_string`, `json_strndup`, `set_xml_code`/`set_json_code`). 9 unit tests; 0 doc-only on both files (parity view clean). portcourt keeps both `partial` -- the closure-math gate would reject a `complete` claim while the library-bound functions remain.
- **`reportio.c` -- 100% 1:1 (39/39, the COBOL Report Writer Control System) -- the 10th of 13 libcob files complete.** The hardest single subsystem in libcob: a control-break state machine over the compiler-built report graph (lines tree -> fields / CONTROLs / SUM counters). The genuinely external pieces are the declared boundary: the `cob_file` `WRITE ... ADVANCING` (`write_rec` instead appends the formatted line bytes to a report output buffer, so the output is observable + testable), and -- a declared NON-CLAIM -- the USE BEFORE/AFTER REPORTING **DECLARATIVES re-entry** (the `ctl`/`use_decl` goto-coroutine path of `cob_report_generate`/`cob_report_terminate`, which requires the cobc-generated USE procedures; the straight-through path a report without USE procedures takes is ported). Everything else is ported + unit-tested: the PAGE LIMIT validation (`cob_report_initiate`: HEADING <= FIRST DETAIL <= LAST DETAIL, FOOTING within bounds), the control-break detection + footing/heading emission + SUM-counter accumulation/zeroing of `cob_report_generate`, `cob_report_terminate`, the line/page output (`write_rec`/`report_line`/`report_line_type`/`report_line_and`/`do_page_heading`/`do_page_footing`), the PRESENT-WHEN control logic (`line_control_one`/`line_control_chg`), the control-break graph queries (`get_control_sequence`/`free_control_fields`/`cob_report_suppress`/`sum_this_counter`/`zero_all_counters`), and the debug dump (`dumpFlags`/`reportDump*`). Verification is structural (11 unit tests of the components -- INITIATE validation, field layout into the record, first-GENERATE heading+detail emission, control-break counter accumulation/zero); a byte-differential REPORT SECTION oracle is future work. portcourt declares reportio.c `complete`; both the typed-parity and doxygen "did-we-miss" compasses confirm it. 10 of 13 libcob files at 100%.
- **`call.c` -- 100% 1:1 (67/67, the CALL/CANCEL program-linkage subsystem) -- the 9th of 13 libcob files complete.** A faithful port of call.c with the genuinely external pieces (dynamic loading `lt_dlopen`/`lt_dlsym`/`lt_dlerror`, the C `setjmp`/`longjmp` stack save/restore, the actual call through a resolved function pointer, and `lt_dlclose`) declared as the OS/ABI boundary, and everything that is a pure function of a field's `(attr, bytes)` ported and unit-tested: the `PROGRAM-ID` name mangling (`cob_encode_program_id`/`cob_encode_invalid_chars` -- leading-digit `_`, `-`->`__`, other invalid byte -> `_HH`, case folding); the field/parameter metadata accessors (`cob_get_field_type`/`size`/`sign`/`scale`/`digits`/`constant` + the `cob_get_param_*` wrappers over a `Params` list); the typed getters/setters (`cob_get_s64_param`/`cob_get_u64_param`/`cob_get_dbl_param`/`cob_get_picx_param`/`cob_get_grp_param` and the `cob_put_*` counterparts, refusing constant parameters); the field<->string conversion (`cob_get_field_str`/`cob_put_field_str` + the `_buffered`/`_param` variants, DISPLAY/PACKED/BINARY rendering + numeric-literal parsing); the resolve cache (`hash` mod 131, `CallCache::insert`/`lookup`, `cob_resolve`/`cob_resolve_cobol`/`cob_resolve_func`/`cob_resolve_internal`); the CALL/CANCEL dispatch (`cob_call`/`cob_func`/`cob_cancel`/`cob_call_field`/`cob_cancel_field`/`cob_set_cancel`/`do_cancel_module`); the path helpers (`cob_chk_dirp`/`cob_chk_call_path`); and the preload/dynload lists (`cache_preload`/`cache_dynload`/`add_to_preload`). 6 unit tests. portcourt now declares call.c `complete` and the gate confirms it. 9 of 13 libcob files at 100% (numeric/move/strings/cconv/termio/cobgetopt/intrinsic/fileio/call).
- **`fileio.c` -- 100% 1:1 (180/180 active functions, the keystone libcob file complete).** The final 34 functions -- the BDB / ISAM / external-file-handler / temp-file-spill interop layer -- transcribed faithfully, with every genuine leaf operation (the Berkeley DB cursor/lock environment, the ISAM `isread`/`ISRECNUM` cursor, the OS temp file, the `callfh` external handler, the raw FCD/KDB pointers) declared as the boundary and the surrounding logic ported: the BDB cursor bookkeeping (`bdb_open_cursor`/`bdb_close_cursor`/`bdb_close_index`/`bdb_nofile`/`bdb_errcall_set`/`bdb_msgcall_set`, contract unit-tested); the FCD pointer accessors (`cob_file_fcd_adrs`/`cob_file_fcdkey_adrs`/`cob_file_fcd_sync`/`cob_fcd_file_sync`/`cob_file_external_addr`); `cob_fd_file_open` (open-flag selection), `cob_file_open` (35/05/00 existence) and `cob_file_close`; `cob_sys_check_file_exist`/`cob_sys_file_info` (real `stat` size, the `localtime` date being the TZ boundary); the sort temp-file spill (`SortSpill`: `cob_create_tmpfile`/`cob_get_sort_tempfile`/`cob_write_block`/`cob_read_item`, the block byte-format round-trip-tested); the `cob_file_sort_using_extfh`/`cob_file_sort_giving_extfh` SORT variants; `get_code_set_converted_data` (CODE-SET table translation, tested); `update_key_from_fcd`/`copy_keys_fcd_to_file`/`get_dupno`/`check_alt_keys` (keyed-backend logic); `savefileposition`/`restorefileposition`/`open_next` (ISAM cursor + concatenated-file position); `join_environment`; and the `EXTFH`/`EXTFH3`/`cob_sys_extfh` opcode dispatcher (`extfh_decode_opcode` maps every `OP_*` opcode, tested). 67 fileio unit tests. fileio.c: the two giants of libcob (numeric.c + intrinsic.c) were already 100%; this completes the third (and the largest by line count after common.c).
- **`fileio.c` -- EXTFH dispatch + ISAM status + FCD registry ported (structural, +21 functions -> 146/182 = 80.2%).** The Micro Focus external-file-handler dispatch layer and the remaining file-runtime helpers, taking `fileio.c` past 80%: the eight `cob_extfh_open`/`cob_extfh_close`/`cob_extfh_start`/`cob_extfh_read`/`cob_extfh_read_next`/`cob_extfh_write`/`cob_extfh_rewrite`/`cob_extfh_delete` wrappers (build the FCD, select the faithful operation opcode -- `OP_OPEN_*`/`OP_CLOSE*`/`OP_READ_SEQ`/`OP_READ_PREV`/`OP_READ_RAN`/`OP_WRITE`/`OP_REWRITE`/`OP_DELETE`/`OP_START_*` -- invoke an external-handler callback, and copy the FCD back; opcode selection unit-tested via a mock handler); `fisretsts` (the ISAM `ISERRNO`->FILE STATUS mapping: 23/10/22/37/35/30/51/52/61/21, unit-tested); `save_fcd_status`; `cob_sync` (organization-dispatched flush, the `fsync`/`isflush`/`DB_SYNC` being the OS boundary); `cob_file_malloc`/`cob_file_free` (key-array RAII); the FCD registry builders `find_fcd`/`find_file`/`find_fcd2` (round-tripped through the existing copy/convert helpers); and the RAII/cache no-ops `free_fcd2`/`free_extfh_fcd`/`freefh`/`cob_cache_file`/`update_record_and_keys_if_necessary`. The external-handler callback, the BDB cursor/lock environment, the KDB key-block, the ISAM-cursor save/restore positioning, and the record/filename pointers remain the declared C-ABI/OS boundary.
- **`fileio.c` -- FCD <-> cob_file conversion ported (structural, +4 functions -> 125/182 = 69.8%).** Faithful 1:1 ports of the File Control Description data conversion: `update_file_to_fcd` (copy a file's status/open-mode/record-lengths/record-mode/organization + the line-sequential feature flags into an FCD3), `update_fcd_to_file` (the reverse, returning the FILE STATUS), `copy_file_to_fcd` (initialise an FCD ahead of an external-handler open: access flags + OPTIONAL flag + MF_CALLFH_GNUCOBOL + mark not-open), and `copy_fcd_to_file` (set a file's organization + access from an FCD). Round-trip-tested (CobFile -> FCD3 -> CobFile preserves organization, access mode, open mode, and record lengths) for SEQ/RELATIVE/INDEXED/LINE-SEQ. The KDB key-block construction, the record/filename pointers, the exception side effect, and the lock-mode field remain the declared C-ABI boundary.
- **`fileio.c` -- key/lock/FCD/RELEASE-RETURN helper layer ported (structural, +11 functions -> 121/182 = 67.6%).** Faithful 1:1 ports extending the sealed INDEXED and SORT courts: the BDB key helpers `bdb_keylen`/`bdb_savekey`/`bdb_setkey`/`bdb_cmpkey`/`bdb_suppresskey` (over the existing `KeyDesc`/`CobFileKey`, unit-tested) and `set_dbt` (the `filename \0 key` record-lock object); the SORT INPUT/OUTPUT PROCEDURE verbs `cob_file_release` (RELEASE -> submit, 00/30) and `cob_file_return` (RETURN -> retrieve, 00/10/30) over the `CobSort` engine, unit-tested; the Micro Focus `fcd2_to_fcd3`/`fcd3_to_fcd2` File Control Description data conversion (32<->64-bit FCD, flag copy + record-length widening + the refKey/lineCount organization swap + the MF_CALLFH_GNUCOBOL flag), round-trip-tested; and `cob_get_filename_print` (the `select ('env' => resolved)` diagnostic string). The KDB key-block construction, the `callfh` external-file-handler dispatch, the BDB cursor/lock environment, and the ISAM-cursor save/restore positioning remain the declared C-ABI/OS boundary.
- **`fileio.c` -- INDEXED organization sealed (`GNURUST.FILEIO.INDEXED.1`).** A faithful port of the COBOL-observable behaviour of the `indexed_*` handlers over a primary RECORD KEY: `indexed_open`/`indexed_write`/`indexed_write_internal`/`indexed_read`/`indexed_read_next`/`indexed_start`/`indexed_start_internal`/`indexed_rewrite`/`indexed_delete`/`indexed_delete_internal`/`indexed_file_delete`/`indexed_close`, plus the record/file locking sub-layer `lock_record`/`unlock_record`/`test_record_lock`/`lock_file`/`unlock_file` (+17 fileio.c functions, now 110/182 = 61.5%). `IndexedStore` is a primary-key-ordered `BTreeMap` model: WRITE rejects a duplicate primary key with 22 (a non-ascending key under SEQUENTIAL access with 21), a random READ returns 00 + record or 23, READ NEXT walks the keys in ascending order (10 at end), START positions the cursor by an =/</<=/>/>= condition (00 or 23), REWRITE of an absent key is 21 (ISAM KEY_INVALID), DELETE is 00/23. The locking sub-layer ports the NOWAIT grant/deny status contract (a record locked by another open is 51, a file 61). Proven by `indexed_sweep` (18/0): a fixed cobc `ORGANIZATION INDEXED` script (write incl duplicate, random read hit/miss, read-next sweep, START =/>/>=/<, rewrite, delete) -- byte-identical FILE STATUS + record bytes + key order. Kani `indexed_write_read_total` + 3 unit tests. The on-disk index format (BDB/VBISAM/EXTFH), ALTERNATE keys, READ PREVIOUS, and the real cross-process BDB lock environment remain the declared OS boundary.
- **`fileio.c` -- SORT/MERGE in-memory engine sealed (`GNURUST.FILEIO.SORTENGINE.1`).** A faithful 1:1 port of libcob's in-memory 4-queue natural-merge sort: `cob_file_sort_init`/`cob_file_sort_init_key`/`cob_file_sort_submit`/`cob_file_sort_process`/`cob_file_sort_retrieve`/`cob_sort_queues`/`cob_new_item`/`cob_file_sort_using`/`cob_file_sort_giving`/`cob_file_sort_giving_internal`/`cob_file_sort_options`/`cob_free_list`/`cob_file_sort_close` (+11 fileio.c functions, now 93/182 = 52.2%). The C singly-linked queues with the `empty` free-list are modelled as an arena linked by `next` indices; `submit` pushes each one-element block onto the shorter input queue, `cob_sort_queues` merges adjacent runs across a 4-queue ping-pong until one sorted run remains, and `retrieve` drains it. The full-key tie breaks by the per-record `unique` insertion counter, so the engine is a STABLE permutation. Proven by `sortengine_sweep` (1/0): a 16-record, full-key-duplicate, mixed ASCENDING/DESCENDING `SORT ... USING/GIVING` -- the engine's drained order is byte-identical to the oracle's GIVING file, several merge rounds deep. Kani `sort_engine_total_and_ordered` + 2 unit tests. The temp-file spill path (above `COB_SORT_MEMORY`), the EXTFH variants, and numeric-key ordering (a composition with `GNURUST.NUMCMP.1`) remain non-claims.
- **`fileio.c` -- filename mapping layer sealed (`GNURUST.FILEIO.MAPPING.1`).** Faithful ports of `cob_chk_file_env` + the simple-case path of `cob_chk_file_mapping`: how an `OPEN` resolves a COBOL `ASSIGN` name to a filesystem path. A bare name (no separator, not absolute) is looked up via `DD_<name>`/`dd_<name>`/`<name>` env vars (with `.`->`_` mangling, or all non-alnum under `COB_ENV_MANGLE`, surrounding quotes stripped); an absolute env value is used as-is; otherwise `COB_FILE_PATH` is prefixed. A name starting with `.`, `-`, or a digit is not env-mapped; an ACU-hyphen name is device-translated. Proven end-to-end by `map_sweep` (2/0): the oracle creates each file at exactly the path the Rust mapping predicts, for both a `DD_`-mapped name and a `COB_FILE_PATH`-prefixed name. Kani `mapping_special_starts_total` + 1 unit test. The complex multi-element path mapping, `COB_ENV_MANGLE` non-default, and the `flag_filename_mapping=off` dialect case remain non-claims.
- **`fileio.c` -- low-dependency leaf functions ported (clang-callgraph-guided).** Using the new `CLANG-AST-PARITY` callgraph to pick the lowest-callee unported definitions, a batch of faithful ports: `cob_findkey`/`cob_findkey_attr` (find the key index whose field matches a query, by record offset, returning the full/part key lengths), `unique_copy` (the 8-byte stable-sort tiebreak key), `cob_sys_mkdir`/`cob_sys_chdir` (the `C$MAKEDIR`/`C$CHDIR` aliases of `cob_sys_create_dir`/`cob_sys_change_dir`), and the `cob_init_fileio`/`cob_exit_fileio`/`cob_exit_fileio_closeall`/`cob_exit_fileio_msg_only` runtime lifecycle (documented no-ops -- libcob's global buffers are RAII / explicit `CobFile` state in the port). `fileio.c` parity 71 -> 80/182 (44.5%).
- **Governance:** the `CLANG-AST-PARITY` report gained footnotes explaining the preprocessed-build totals (lower than the source-symbol inventory for `#if 0`/config-gated/macro-shaped paths) and the `ported` cross-reference (name match vs the authoritative symbol parity), plus the planned `unclassified calls` classification column.
- No API breaks: additive (new `fileio` helper/alias/lifecycle functions).

## [0.7.51]
- **`fileio.c` -- file runtime OPEN/CLOSE + lifecycle sealed (`GNURUST.FILEIO.OPEN.1`).** A `CobFile` runtime ties the sealed organization handlers into a working file: faithful ports of `cob_open`/`cob_close`/`cob_pre_open`/`cob_unlock`/`cob_file_unlock`/`cob_unlock_file`/`cob_commit`/`cob_rollback`/`cob_delete_file`, proven to match the admitted GnuCOBOL 3.2 `libcob`'s FILE STATUS and on-disk file image (`open_sweep` 2/0, end-to-end against an OPEN OUTPUT/WRITE/READ/CLOSE LINE SEQUENTIAL program):
  - `cob_open` loads the file image (real `std::fs` I/O) and sets the status -- a file closed-with-lock is `38`, an already-open file `41`, an empty/badly-quoted filename `31`, OPEN INPUT of a missing file `35` (or `05` when `OPTIONAL`), OPEN OUTPUT truncates; `cob_close` flushes an output/I-O image and is `42` when the file is not open, leaving it `Locked` for `CLOSE WITH LOCK`; `WRITE`/`READ NEXT` dispatch by organization to the sealed `sequential_*`/`lineseq_*`/`relative_*` handlers.
  - The indexed/BDB and SORT organizations' open, real record/file locking, OPEN EXTEND positioning, file-sharing, and the EXTFH path stay explicit non-claims. `fileio.c` parity 62 -> 71/182 (39.6%).
- No API breaks: additive (new `fileio::CobFile` + `cob_open`/`cob_close`/lifecycle functions + `OpenMode::Locked`).

## [0.7.50]
- **`fileio.c` -- CBL_* handle-based byte-stream file routines + C$DELETE/C$COPY (extends `GNURUST.FILEIO.SYS.1`).** Ports `open_cbl_file`/`cob_sys_open_file`/`cob_sys_create_file`/`cob_sys_read_file`/`cob_sys_write_file`/`cob_sys_close_file`/`cob_sys_flush_file` (the `CBL_OPEN_FILE`/`CREATE_FILE`/`READ_FILE`/`WRITE_FILE`/`CLOSE_FILE`/`FLUSH_FILE` routines) plus `cob_sys_file_delete`/`cob_sys_copyfile` (`C$DELETE`/`C$COPY`), proven against the oracle (`cob_sys_sweep` 2/0, now incl. a `CBL_CREATE_FILE`/`WRITE_FILE`/`READ_FILE` round-trip that reads back the written bytes):
  - the opaque 4-byte handle is a safe `File`-registry index (libcob stores the raw `fd`; `#![forbid(unsafe_code)]` precludes that, and the handle is opaque to the program so behaviour is identical); access `1` read / `2` write+create+truncate / `3` r/w; positioned read/write (status `0`/`10`/`30`/`-1`); a `flags & 0x80` size query.
  - **Forensic note:** `CBL_READ_FILE`/`WRITE_FILE` decode the offset/length parameters as **big-endian** (libcob byte-swaps them), so the oracle fields must be `COMP`, not `COMP-5` -- a `COMP-5` length of 5 is byte-swapped to 83886080 and the write returns status 30. `fileio.c` parity 53 -> 62/182 (34.6%).
- No API breaks: additive (new `fileio::cob_sys_*` handle routines + `C$DELETE`/`C$COPY` wrappers).

## [0.7.49]
- **`fileio.c` -- CBL_* system file/directory routines sealed (`GNURUST.FILEIO.SYS.1`).** A faithful port of `libcob/fileio.c`'s `cob_sys_delete_file`/`cob_sys_copy_file`/`cob_sys_rename_file`/`cob_sys_create_dir`/`cob_sys_delete_dir`/`cob_sys_change_dir`/`cob_sys_get_current_dir` -- the `CBL_DELETE_FILE`/`CBL_COPY_FILE`/`CBL_RENAME_FILE`/`CBL_CREATE_DIR`/`CBL_DELETE_DIR`/`CBL_CHANGE_DIR`/`CBL_GET_CURRENT_DIR` library routines a COBOL program `CALL`s -- proven to match the admitted GnuCOBOL 3.2 `libcob`'s `RETURN-CODE` (`cob_sys_sweep` 1/0, end-to-end against a fixed CALL sequence):
  - each performs the syscall (via `std::fs`/`std::env`, as the core already does for collation/clock I/O) and returns `0` success, `128` failure, `35` when a copy source is absent, `129` for `CBL_GET_CURRENT_DIR` with nonzero flags, `-1` for a missing parameter; `CBL_GET_CURRENT_DIR` writes the cwd space-filled and double-quoted when it contains a space.
  - The `localtime`-dependent `CBL_CHECK_FILE_EXIST` date formatting, the handle-based `CBL_OPEN`/`READ`/`WRITE`/`CLOSE`/`FLUSH_FILE` routines, and the filename-mapping path stay explicit non-claims. `fileio.c` parity 46 -> 53/182 (29.7%).
- No API breaks: additive (new `fileio::cob_sys_*` routines).

## [0.7.48]
- **`fileio.c` -- SORT/MERGE record comparison sealed (`GNURUST.FILEIO.SORT.1`).** A faithful port of `libcob/fileio.c` `sort_cmps` + `cob_file_sort_compare` + `cob_file_sort_init_key` -- the record order a `SORT` produces -- proven to match the admitted GnuCOBOL 3.2 `libcob`'s GIVING-file order (`sort_sweep` 1/0, end-to-end against a real `SORT ON ASCENDING KEY ... ON DESCENDING KEY ...` with duplicate keys):
  - each `ON ASCENDING`/`DESCENDING KEY` is a byte range of the record compared via `sort_cmps` (byte-by-byte, optionally through a 256-entry collating table); the first differing key decides, negated for DESCENDING; a full key tie breaks by insertion order (the `unique` field) so the sort is **stable**.
  - Numeric keys (routed through `cob_numeric_cmp`, a declared composition with `GNURUST.NUMCMP.1`), the sort engine's tempfile merge/queues, INPUT/OUTPUT PROCEDURE, and MERGE stay explicit non-claims. `fileio.c` parity 43 -> 46/182 (25.8%).
- No API breaks: additive (`fileio::sort_cmps`/`cob_file_sort_compare`/`cob_file_sort_init_key`/`sort_records` + `SortKey`).

## [0.7.47]
- **`fileio.c` -- verb open/access-mode preconditions sealed (`GNURUST.FILEIO.VERB.1`).** A faithful port of the precondition layer of `libcob/fileio.c` `cob_write`/`cob_read`/`cob_read_next`/`cob_rewrite`/`cob_delete`/`cob_start` -- the FILE STATUS a verb returns *before* dispatching to its organization handler -- proven byte-identical to the admitted GnuCOBOL 3.2 `libcob` (`verb_sweep` 7/0):
  - WRITE needs `OPEN OUTPUT`/`EXTEND` in sequential access (otherwise `OUTPUT`/`I-O`), else `48`; READ and READ NEXT need `OPEN INPUT`/`I-O`, else `47`; REWRITE and DELETE need `OPEN I-O`, else `49`; a SEQUENTIAL-access REWRITE/DELETE without a prior successful READ is `43`; a sequential READ past end-of-file is `46`; a record outside `record_min..record_max` is `44`.
  - The organization dispatch itself is sealed separately (LINESEQ.1/.2, SEQ.1, RELATIVE.1); the compile-time `START`-on-RANDOM rejection (cobc rejects it before runtime), CODE-SET conversion, and the verb-layer side effects stay explicit non-claims. `fileio.c` parity 37 -> 43/182 (24.2%).
- No API breaks: additive (`fileio::cob_write`/`cob_read`/`cob_read_next`/`cob_rewrite`/`cob_delete`/`cob_start` + `OpenMode`/`AccessMode`/`Organization` enums).

## [0.7.46]
- **`fileio.c` -- record-I/O helper layer ported; the file is now past 20% (37/182).** A batch of faithful, unit-tested structural ports of the pure byte/decision helpers the sealed organizations build on (no new oracle court -- the byte-courts remain LINESEQ.1/.2, SEQ.1, RELATIVE.1):
  - **INDEXED key descriptors** -- `indexed_keylen`/`indexed_savekey`/`indexed_restorekey`/`indexed_cmpkey`/`indexed_keycmp`/`indexed_keydesc` + `cob_savekey` extract, restore and compare multi-part keys from a record per a key descriptor.
  - **WRITE ADVANCING + LINAGE** -- `cob_seq_write_opt`/`cob_file_write_opt` (the advancing newline/CR/form-feed bytes), `cob_linage_write_opt` (page advancing + LINAGE-COUNTER), `file_linage_check` (linage geometry validation), and `lineseq_rewrite` (in-place LINE SEQUENTIAL REWRITE).
  - **path / status / record helpers** -- filename classification (`is_absolute`/`looks_absolute`/`has_directory_separator`/`has_acu_hyphen`/`do_acu_hyphen_translation`), `errno_cob_sts` (errno to FILE STATUS), `save_status` (status to 2 display bytes), `cob_str_from_fld` (right-trim + de-quote), `cob_copy_check` (SORT record move), `is_suppressed_key_value`, and the `dummy_*` no-op handlers.
- No API breaks: additive (new `fileio` functions + `KeyDesc`/`KeyPart`/`CobFileKey`/`Linage`/`FileErrno` types).

## [0.7.45]
- **`fileio.c` — RELATIVE organization sealed (`GNURUST.FILEIO.RELATIVE.1`).** A faithful port of `libcob/fileio.c` `relative_write`/`relative_read`/`relative_read_next`/`relative_rewrite`/`relative_delete`/`relative_start`, proven byte-identical to the admitted GnuCOBOL 3.2 `libcob` (`relative_sweep` 4/0):
  - an `ORGANIZATION IS RELATIVE` file is an array of fixed slots of `sizeof(record->size) + record_max` bytes — each an **8-byte native-endian size header** (`> 0` active, `0` empty/deleted) followed by the `record_max` data area; the record at relative key *N* lives at slot *N-1*.
  - keyed `WRITE` writes the slot and zero-fills any gap (status `22` on an occupied slot, `24` on key `< 1`); `READ`/`REWRITE`/`DELETE` address slot `key-1` (`23` on an empty slot, `24` on key `< 1`); **`DELETE` tombstones by zeroing the header and leaves the data bytes intact**; sequential `READ NEXT` skips empty/deleted slots and is end-of-file (`10`) at the end; `START` positions by `EQ`/`GE`/`GT`/`LE`/`LT`/`FIRST`/`LAST`.
  - The `fd` `lseek`/`read`/`write` syscalls remain the declared OS boundary; variable-length relative records and the `READ FIRST`/`LAST`/`PREVIOUS` directions stay explicit non-claims. `fileio.c` parity 6 → 12/182.
- No API breaks: additive (`fileio::relative_*` + `RelWrite`/`RelRead`/`RelCond`/`relsize`/`REL_SIZE_HEADER`).

## [0.7.44]
- **`fileio.c` — RECORD SEQUENTIAL read/write incl. variable-length records sealed (`GNURUST.FILEIO.SEQ.1`).** A faithful port of `libcob/fileio.c` `sequential_write` + `sequential_read` + `set_sequential_variable_length` (+ `sequential_rewrite`), proven byte-identical to the admitted GnuCOBOL 3.2 `libcob` (`seqrec_sweep` 9/0):
  - a **fixed** record (`record_min == record_max`) is written as the full record area with no delimiter and read back in `record_max` chunks — a short final read overwrites only the bytes available and **leaves the prior record's tail** (status `00`), and a zero-byte read is end of file (`10`).
  - a **variable-length** record (`RECORD IS VARYING`) is framed by a `cob_varseq_type` length prefix whose width and byte order depend on **`COB_VARSEQ_FORMAT`**: `0`/default = `BE16(size)` + two NUL bytes (4 bytes), `1` = `BE32` (4), `2` = native little-endian `LE32` (4), `3` = `BE16` (2) — all four verified on both WRITE (raw prefix bytes) and READ (status + size round-trip).
  - The `fd` read/write/lseek syscalls remain the declared OS boundary; `WRITE ... ADVANCING`, CODE-SET conversion, the over-long-record `bytes_to_skip` seek-past, and the relative/indexed organizations stay explicit non-claims. `fileio.c` parity 3 → 6/182 (plus the not-separately-counted `set_sequential_variable_length`).
- No API breaks: additive (`fileio::sequential_write`/`sequential_read`/`sequential_rewrite`/`set_sequential_variable_length`/`cob_vsq_len`).

## [0.7.43]
- **`fileio.c` — line-sequential `READ` config matrix sealed (`GNURUST.FILEIO.LINESEQ.2`).** The READ counterpart to 0.7.42's WRITE court: a faithful port of `libcob/fileio.c` `lineseq_read` (the record bytes and FILE STATUS produced by `READ ... NEXT RECORD` over an `ORGANIZATION IS LINE SEQUENTIAL` file), proven byte-identical to the admitted GnuCOBOL 3.2 `libcob` across the `COB_LS_*` matrix (`lineseq_read_sweep` 12/0):
  - a line ends at `\n`; `\r\n` folds to `\n`; a lone `\r` is kept as a data byte; a short line is space-filled (`00`); a trailing newline makes no empty record; a mid-file empty line **is** a record; EOF with no bytes read is status `10`.
  - **`COB_LS_VALIDATE`** flags an `IS_BAD_CHAR` byte with status `09` (the line is still delivered); **`COB_LS_NULLS`** decodes a `0x00`-prefixed control byte; **`COB_LS_SPLIT`** on splits a long line into `06`+`00` records, off truncates to status `04` and discards the rest.
  - **Forensic asymmetry (both verified across `0x00–0x1F`):** `IS_BAD_CHAR`'s `BS`/`TAB`/`FF`/`SI`/`ESC` exclusions are **live on READ** (those five → status `00`) but **dead on WRITE** (every byte `< 0x20` → status `71`) — the same source macro, opposite effective behavior in the compiled 3.2 GA `libcob`.
  - The `fd`/`FILE*` reads remain the declared OS boundary; the multi-file `open_next` chain, CODE-SET conversion, `COB_LS_VALIDATE>1`, and `lineseq_rewrite` stay explicit non-claims. `fileio.c` parity 2 → 3/182.
- No API breaks: additive (`fileio::lineseq_read`/`read_line_sequential` + a new field `LineSeqConfig::ls_split`, default `true`).

## [0.7.42]
- **`fileio.c` opened — line-sequential `WRITE` config matrix sealed (`GNURUST.FILEIO.LINESEQ.1`).** The first sub-surface of `libcob/fileio.c` (file 8/13, the 10.7k-line keystone): a faithful port of `lineseq_size` + `lineseq_write` (the bytes a `WRITE` appends to an `ORGANIZATION IS LINE SEQUENTIAL` file and the resulting FILE STATUS), proven byte-identical to the admitted GnuCOBOL 3.2 `libcob` across the `COB_LS_*` runtime-config matrix (`lineseq_write_sweep` 8/0):
  - **default (`COB_LS_VALIDATE=1`)** strips trailing spaces, writes the record raw + `\n`, but rejects any record byte `< 0x20` with status `71` (writing nothing); **`COB_LS_VALIDATE=0`** passes control bytes through raw; **`COB_LS_NULLS=1`** emits `0x00` before each byte `< 0x20`; **`COB_LS_FIXED=1`** writes the full unstripped record area.
  - **Forensic finding:** `fileio.c`'s `IS_BAD_CHAR` macro excludes `BS`/`ESC`/`FF`/`SI`/`TAB`, but those exclusions are **dead** in the compiled 3.2 GA `libcob` — every byte `0x00–0x1F` is rejected (verified across the full `0x00–0xFF` range; the binary is the authority).
  - In keeping with the pure-kernel model (`#![forbid(unsafe_code)]`), the actual `fd`/`FILE*` syscalls are the declared OS boundary: the kernel decides the bytes, the caller performs the I/O. `WRITE ADVANCING`/LINAGE, the Windows CR/LF path, `lineseq_read`/`lineseq_rewrite`, and the other organizations remain explicit non-claims for follow-up courts.
- This closes the `COB_LS_FIXED`/`COB_LS_NULLS` line modes that `GNURUST.FILE.WRITE.1` had left not-proven.
- No API breaks: additive (new `fileio` module + court; new internal fuzz target).

## [0.7.41]
- **`intrinsic.c` completed -- file 7/13 is now 100% 1:1** (233/233 active functions; the second fully-1:1 `libcob` file after `numeric.c`). Each piece oracle-verified byte-for-byte against the admitted GnuCOBOL 3.2 `libcob` -- the `cob_intr_*` differential sweep grew to 220 cases, all passing:
  - **mpf-transcendental layer** -- a pure-Rust 2048-bit `Mpf` reproduces libcob's `cob_mpf_*` series byte-for-byte: `SQRT`/`EXP`/`EXP10`/`LOG`/`LOG10`/`SIN`/`COS`/`TAN`/`ASIN`/`ACOS`/`ATAN`/`PI`/`E` + `cob_decimal_pow`, plus `BINOP`, financial `ANNUITY`/`PRESENT-VALUE`, and `VARIANCE`/`STANDARD-DEVIATION`. Matching GMP precision required round-to-nearest `mpf_get_str`, the GMP `mpz_sizeinbase` field-width estimate, and a wide-DISPLAY render path -- with the sealed `numeric.c`/FLOAT courts held intact.
  - **NUMVAL family + the formatted date/time cluster** -- `TEST-NUMVAL`/`-C`/`-F`, `NUMVAL-F`, `INTEGER-OF-FORMATTED-DATE`, `FORMATTED-DATE`/`TIME`/`DATETIME`, `TEST-FORMATTED-DATETIME`, `SECONDS-FROM-FORMATTED-TIME` (a ~70-helper date / ISO-week / format-string web).
  - **runtime / clock / locale introspection** -- `CURRENT-DATE`/`FORMATTED-CURRENT-DATE` (via a ported `cob_get_current_datetime` + `COB_CURRENT_DATE` parser for determinism), `MODULE-*`, `EXCEPTION-*`, `LOCALE-DATE`/`TIME`/`COMPARE`/`-FROM-SECONDS`, `WHEN-COMPILED`, `CONTENT-LENGTH`/`OF`, `RANDOM`, plus every internal helper under its exact C name.
  - Two declared boundaries, consistent with the existing GMP-substrate policy: a non-null COBOL `POINTER` dereference (`#![forbid(unsafe_code)]`) and `RANDOM`'s exact bit-stream (GMP's internal Mersenne Twister -- verified not the textbook MT19937 seeding).
- **`GNURUST.CCVS85.1` + `GNURUST.COBOL-CORPUS-ATLAS.1` -- external COBOL corpus custody gates.** Admit the historical NIST **CCVS85** COBOL-85 validation suite and a 5-corpus atlas across three evidence classes -- historical conformance (CCVS85), upstream compiler regression (GnuCOBOL `tests/`, GCC `gcobol`), and independent real-world / defect corpora (OpenCBS, X-COBOL). **Custody/index only -- no conformance, suite-pass, or behaviour-parity claim** (compile/run baselines deferred to tiered `.2`/`.3`/`.4` gates). New `gnucobol-rs-port-index` subcommands `ccvs85` + `corpus-atlas` (a pure-Rust SHA-256, no new deps), each freshness-gated into the verification + docs-staleness guards; raw corpora are re-downloadable from permanent sources, the manifests are the committed spine.
- No API breaks: every change is additive (new `intrinsic` functions throughout; new internal port-index subcommands).

## [0.7.40]
- **Files 1-5 of libcob sealed + the typed port-index.** Batch release accumulating the completion of the first five `libcob` source files as faithful 1:1 Rust ports, each oracle-verified byte-for-byte against the admitted GnuCOBOL 3.2 `libcob`:
  - **`numeric.c`** completed (the decimal/GMP layer: real pure-Rust `Mpz` + `Mpf`, packed BCD arithmetic, the `#if 0` functions) -- `GNURUST.NUMERIC.CLOSE.1`.
  - **`move.c`** 57/57 (the `cob_move` conversions + the `cob_get/put_*` accessor API + edited leaves) -- `GNURUST.MOVE.CLOSE.1`.
  - **`strings.c`** 34/34 (the STRING / UNSTRING / INSPECT statement state machines as explicit structs, no global state) -- `GNURUST.STRINGS.CLOSE.1`.
  - **`cobgetopt.c`** 4/4 (GnuCOBOL's vendored GNU `getopt_long`, scanner state in an explicit struct) -- `GNURUST.COBGETOPT.CLOSE.1`; `getopt_sweep` 35/0.
  - **`cconv.c`** 9/9 (7-bit ASCII case fold, hex/whitespace scanners, `cob_field_to_string`, `.ttbl` collation loading) -- `GNURUST.CCONV.CLOSE.1`; `cconv_sweep` 27/0.
- **PORT-INDEX.1 -- typed C<->Rust symbol parity** (`gnucobol-rs-port-index`, an internal `publish = false` governance crate). Replaces grep name-matching: it classifies each libcob C function by preprocessor status (`#if 0` / config-gated) and joins it against a comment/string-aware real-`fn` index of the port, so a name appearing only in a doc comment is a `doc_only` false hit, never a port. PORT-INDEX.1a then closed every false hit it exposed in the completed files (giving each faithful counterpart its exact C name). Plus a first-class C-vs-Rust **doxygen** coverage gate (`DOXYGEN-PARITY.md`). Both wired into the verification + docs-staleness gates.
- No API breaks: every change is additive (new modules `cconv`, `cobgetopt`; new functions). The published `0.7.39` only contained the first numeric.c court (`NUMCMP.1`); this release ships the rest of files 1-5.

## [0.7.39]
- **GNURUST.NUMCMP.1** numeric comparison sealed -- and the first court of a true 1:1 port of numeric.c onto a pure-Rust GMP subset. New `gmp::Mpz` (arbitrary-precision signed integer, the ~40 mpz_* ops numeric.c uses, zero deps) + `cob_decimal` layer (set_field/align/shift/add/sub/mul/div/do_round/get_field/cmp). cob_numeric_cmp's -1/0/1 verdict is byte-identical to libcob across DISPLAY/PACKED x scales x signs (incl. -0): numcmp_sweep 1024/0. The cob_decimal MULTIPLY path is also oracle-verified (1800/0 + bignum 16128/0 -- the arbitrary-precision Mpz subsumes the i128+u256 special-casing) and DIVIDE cross-checks the proven path.

## [0.7.38]
- **GNURUST.FLOAT.1** floating-point fields sealed: COMP-1 (IEEE single), COMP-2 (IEEE double), and FLOAT-DECIMAL-16/34 (IEEE-754-2008 decimal64/128, BID), both directions, byte-exact vs cob_move (`float_sweep` 1476/0). The forensic finding: GnuCOBOL converts decimal->binary-float by TRUNCATING TOWARD ZERO (GMP mpf_get_d), so an inexact decimal lands 1 ULP below a correctly-rounded parse; COMP-1 truncates to double then narrows round-to-nearest (the C (float) cast). Reproduced with exact big-integer comparisons (new BigU), no float roundtrips of decimal data (NO-FLOAT-DECIMAL.0 refined, not retired). Non-claims: float arithmetic, cob_cmp_float, long double, FP_BIN32/64/128, Inf/NaN beyond decode-refusal.

## [0.7.37]
- **numeric.c completion (3 courts): GNURUST.BIGNUM.1 + GNURUST.INTPOW.1 + GNURUST.LOGICAL.1.** MULTIPLY beyond i128 now carries the exact 256-bit product and truncates to the receiver's low digits instead of failing closed (full binary-multiply domain incl. combined-scale >38; bignum_sweep 16128/0). Integer `**` via cob_s32_pow/cob_s64_pow with libcob's exact edges (base==-1->1, negative power->0, wrapping overflow; pow_sweep 588/0). Bit-logical B-AND/B-OR/B-XOR/B-NOT + shifts over `|value| mod 2^64` (sign discarded, shift mod 64; logical_sweep 2400/0). All proven byte/value-identical vs the real libcob.

## [0.7.36]
- **GNURUST.ROUND.1 (ROUND.2 extension)** packed `ROUNDED MODE IS` sealed: ADD/SUBTRACT into a COMP-3 receiver takes libcob's `cob_add_bcd` nibble rounding (numeric.c:2826+), which matches the cob_decimal path for every mode except NEAREST-EVEN -- the BCD path resolves that tie away-from-zero (no to-even). Fixes a divergence 0.7.35 shipped (e.g. `ADD ... ROUNDED MODE IS NEAREST-EVEN` of 2.5 into packed = 3, not 2). `round_sweep` broadened to DISPLAY + packed receivers across kept digits 0-9, 6720/0, mutation-verified. Non-claims unchanged: bignum >i128, float COMP-1/2.

## [0.7.35]
- **GNURUST.ROUND.1** ROUNDED MODE IS (all eight rounding modes) sealed: a value narrowed to a smaller scale under each `ROUNDED MODE IS` setting on the cob_decimal store path -- NEAREST-AWAY-FROM-ZERO (the default ROUNDED), AWAY-FROM-ZERO, NEAREST-EVEN (banker's), NEAREST-TOWARD-ZERO, TOWARD-GREATER (ceiling), TOWARD-LESSER (floor), PROHIBITED (size error on a dropped non-zero digit), and TRUNCATION -- byte-faithful to cobc (`cob_decimal_do_round`); `round_sweep` 672/0 + fuzz + Kani. Non-claims: ADD/SUBTRACT into a packed field (cob_add_bcd nibble rounding, GNURUST.13's surface), bignum beyond i128, floating-point COMP-1/COMP-2.

## [0.7.34]\n- **GNURUST.INDEX.1** USAGE INDEX storage + SET arithmetic sealed: a USAGE INDEX / INDEXED BY item is a 4-byte native-endian (little-endian on the x86-64 oracle) signed word holding the **occurrence number** (element-size independent -- `SET IDX TO 5` stores 5, never 5*element_size), and `SET ... TO/UP BY/DOWN BY` is plain integer arithmetic (DOWN past zero stores the two's-complement negative; cobc does not clamp); `index_sweep` 41/0 + fuzz + Kani. Non-claims: 8-byte LP64 index width, SEARCH execution, index-as-subscript stride multiply (lives in GNURUST.SUBSCRIPT.1).\n\n## [0.7.33]\n- **GNURUST.ODO.1** OCCURS DEPENDING ON sealed: the used length of a variable-length group (`fixed_prefix + N*element_size`, matching cobc `LENGTH OF`) and bounded element access `E(i)` for `1<=i<=N`, fail-closed beyond the active count; `odo_sweep` 10/0 + fuzz + Kani.\n\n## [0.7.32]\n- **GNURUST.SUBSCRIPT.1** table subscript access sealed: 1-based `TABLE(i)` and multi-dimensional `TABLE(i,j)` element extraction (offset = sum (idx-1)*stride), byte-exact in bounds and fail-closed out of bounds; `subscript_sweep` 17/0 + fuzz + Kani.\n\n## [0.7.31]\n- **GNURUST.REFMOD.1** reference modification sealed: `field(start:length)` substring + `field(start:)` to-end + the receiver `MOVE src TO field(start:length)` (1-based, alphanumeric overwrite), byte-exact in bounds and **fail-closed out of bounds** (safer than cobc's flag-dependent read-past); `refmod_sweep` 16/0 + fuzz + Kani.\n\n## [0.7.30]\n- **GNURUST.CLASS.1** class conditions sealed: `IF data-item IS NUMERIC / ALPHABETIC / ALPHABETIC-UPPER / ALPHABETIC-LOWER` over an alphanumeric field, byte-faithful to cobc (digits-only / letters-or-space); `class_sweep` 26/0 + fuzz + Kani.\n\n## [0.7.29]\n- **GNURUST.12B** `SET condition-name TO FALSE` sealed (the inverse of GNURUST.12): `cond::set_88_false` writes the `WHEN SET TO FALSE IS` literal into the parent, byte-faithful to cobc; `set_false_sweep` 37/0 + fuzz + Kani.\n\n## [0.7.28]\n- **GNURUST.16C** numeric->edited ENCODE sealed (the inverse of GNURUST.16 decode): `edited::encode_edited` formats a value into edited DISPLAY bytes byte-faithful to cobc (zero-suppression, floating `$`, fixed `+/-`, `* CR DB B 0 /`); `edited_encode_sweep` 92/0 + fuzz + Kani. Non-claims: floating sign strings, locale CURRENCY, EBCDIC edited.\n\n## [0.7.26]\n- **GNURUST.SEARCH.TABLE.1 — SEARCH / SEARCH ALL table lookup (PATCH).** New `search::search_serial / search_all`: the 1-based landing index of a table search. SEARCH (serial) scans **forward from the current index**; SEARCH ALL **binary-searches the ascending key**. A real byte court (Kani soundness proof + fuzz). Proven: `search_sweep` 6/0 + 2 unit tests. Non-claims: DESCENDING/multi keys, non-numeric keys, unsorted SEARCH ALL, the WHEN/AT END bodies.\n\n## [0.7.25]\n- **GNURUST.TABLE.PERFORM.SLICE.1 — table (OCCURS) PERFORM VARYING slice (PATCH).** New `table_slice::eval_table_loop`: runs `PERFORM VARYING I … TABLE(I)` over a 1-based `OCCURS` table, accumulating (sum/count, optional per-element filter). Proven: `table_sweep` 3/0 + 3 unit tests. The subscript is **1-based** (`TABLE(1)` = first element). Non-claims: multi-dim/nested OCCURS, OCCURS DEPENDING ON, out-of-bounds, INDEXED/SEARCH, signed elements.\n\n## [0.7.24]\n- **GNURUST.IF.NUMERIC.SLICE.1 — numeric IF/EVALUATE slice (PATCH).** New `if_numeric::eval_if_numeric / eval_evaluate_numeric`: the numeric counterpart of the alphanumeric IF.EVALUATE.SLICE.1 -- compares 9(n) field VALUES (so `5<10`) and applies numeric MOVE branches. Proven: `if_numeric_sweep` 7/0 + 2 unit tests. Completes conditional logic for both data classes. Non-claims: signed/V numerics, field-to-field MOVE, SIZE ERROR, compound conditions, range WHEN.\n\n## [0.7.23]\n- **GNURUST.FILE.FILTER.SLICE.1 — filter (conditional) read-loop (PATCH).** New `file_flow_slice::eval_filter_loop`: the read-loop with a per-record `IF` gating accumulation -- numeric (compare value: `R-AMT>=50`) or alphanumeric (byte compare). The selective-accumulation workhorse of batch. Proven: `file_filter_sweep` 4/4 + 1 unit test. Non-claims: compound AND/OR, signed numeric filter, per-record mutation, EVALUATE classifier.\n\n## [0.7.22]\n- **GNURUST.FILE.FLOW.SLICE.1 — read-loop execution slice (PATCH).** New `file_flow_slice::eval_read_loop`: EXECUTES the canonical `OPEN INPUT / PERFORM UNTIL EOF: READ + accumulate / CLOSE` loop to oracle-identical WORKING-STORAGE -- where file I/O meets control flow (the shape of every COBOL batch job). Composes the sealed sequential READ with looped accumulation; each record processed exactly once. Proven: `file_flow_sweep` 2/2 + 2 unit tests. **Completes the parity spine.** Non-claims: indexed/relative, per-record IF/MOVE, WRITE in loop, multi-file, signed accumulators.\n\n## [0.7.21]\n- **GNURUST.PERFORM.SLICE.1 — PERFORM execution slice (PATCH).** New `perform_slice::eval_perform`: EXECUTES `PERFORM n TIMES / UNTIL / VARYING` over numeric counters to oracle-identical storage. Test-before; VARYING leaves the control variable **one step past** the limit. Proven: `perform_sweep` 10/0 + 3 unit tests. Non-claims: signed/packed counters, body SIZE ERROR, non-ADD bodies, compound conditions, THRU/paragraph, TEST AFTER, nesting.\n\n## [0.7.20]\n- **GNURUST.IF.EVALUATE.SLICE.1 — first execution slice (PATCH).** New `if_eval::eval_if / eval_evaluate`: a narrow interpreter that EXECUTES an `IF`/`EVALUATE` fragment over alphanumeric fields (single relation, alphanumeric compare, MOVE branches) and yields oracle-identical storage. Composes the sealed compare + MOVE courts under control flow (the atlas only observed it). Proven: `if_eval_sweep` 9/0 + 4 unit tests. Non-claims: numeric comparison, compound/class conditions, 88-level, non-MOVE branches, nesting.\n\n## [0.7.19]\n- **GNURUST.INTRINSIC.DATE.1 — date-conversion intrinsics (PATCH).** New `intrinsic::intrinsic_integer_of_date / date_of_integer / integer_of_day / day_of_integer`: proleptic-Gregorian day numbers from a fixed epoch (1601-01-01 = 1), the four functions mutual inverses. Deterministic (distinct from the refused env-sensitive CURRENT-DATE). Proven byte-identical: `date_sweep` 30/0 + a 200000-day round-trip test. Non-claims: invalid-date validation, CURRENT-DATE/WHEN-COMPILED, Y2K windowing.\n\n## [0.7.18]\n- **GNURUST.FILE.REWRITE.1 — sequential REWRITE in-place update (PATCH).** New `file_seq::rewrite_records`: `OPEN I-O` + `REWRITE` overwrites the current record **in place** (same length), leaving every other record unchanged. The update side of the file court. Proven byte-identical: `rewrite_sweep` 1/0 + 1 unit test. Non-claims: LINE SEQUENTIAL REWRITE, length-changing rewrites, DELETE, indexed/relative.\n\n## [0.7.17]\n- **GNURUST.FILE.WRITE.1 — sequential WRITE byte effects (PATCH).** New `file_seq::write_sequential`: RECORD SEQUENTIAL writes the full fixed padded record (no delimiter); LINE SEQUENTIAL **strips trailing spaces** + adds a newline (all-spaces record → just a newline). The WRITE side of FILE.SEQUENTIAL.1. Proven byte-identical: `write_seq_sweep` 2/0 + 2 unit tests. Non-claims: configured line modes, variable-length, WRITE ADVANCING, REWRITE, indexed/relative.\n\n## [0.7.16]\n- **GNURUST.SIZE.ERROR.1 — arithmetic SIZE ERROR (PATCH).** New `size_error::arith_size_error`: overflow stores the **low-order** truncated digits (high digits dropped: `1998`→`998` in `9(3)`), and signals the size-error condition (with `ON SIZE ERROR` the receiver is unchanged). Implements the observed SIZE.ERROR.ATLAS.1. Proven byte-identical: `size_error_sweep` 12/0 + 2 unit tests. Non-claims: the arithmetic itself (GNURUST.7/13/19), ROUNDED, SIZE ERROR on MOVE.\n\n## [0.7.15]\n- **GNURUST.ACCEPT.DISPLAY.2 — DISPLAY of signed/V-scaled numeric (PATCH).** New `accept_display::display_numeric`: a signed field prints a `+`/`-` prefix (positive zero is `+`), a V-scaled field inserts a `.`. Seals what ACCEPT.DISPLAY.1 deferred. Proven byte-identical: `accept_display2_sweep` 8/0 + 1 unit test. Non-claims: numeric-edited PICs (GNURUST.16), BLANK WHEN ZERO, JUSTIFIED.\n\n## [0.7.14]\n- **GNURUST.INTRINSIC.NUMVAL-C.1 — FUNCTION NUMVAL-C (PATCH).** New `intrinsic::intrinsic_numval_c`: like NUMVAL but strips the default `$` currency symbol and thousands commas (NUMVAL-C("$1,234.56")=1234.56). Proven byte-identical: `numvalc_sweep` 10/0 + 1 unit test. Non-claims: non-default currency symbol, locale comma-decimal, national/UTF-8.\n\n## [0.7.13]\n- **GNURUST.INTRINSIC.ORD-CHAR.1 — FUNCTION ORD/CHAR (PATCH).** New `intrinsic::intrinsic_ord / intrinsic_char`: 1-based ordinal (ORD('A')=66) and its inverse (CHAR(66)='A'). Proven byte-identical incl the full ORD(CHAR(n))=n round-trip: `ordchar_sweep` 15/0 + 1 unit test. Non-claims: non-default collating sequences, national/UTF-8, CHAR out of 1..256. **Every deterministic atlas intrinsic is now implemented.**\n\n## [0.7.12]\n- **GNURUST.INTRINSIC.CASE.1 — FUNCTION UPPER-CASE/LOWER-CASE/REVERSE (PATCH).** New `intrinsic::intrinsic_upper_case / lower_case / reverse`: ASCII case fold (non-alpha unchanged) + byte reversal (spaces included). Proven byte-identical: `case_sweep` 10/0 + 1 unit test. Non-claims: locale/national case folding, multibyte REVERSE.\n\n## [0.7.11]\n- **GNURUST.INTRINSIC.INTEGER.1 — FUNCTION INTEGER / INTEGER-PART (PATCH).** New `intrinsic::intrinsic_integer (floor) / intrinsic_integer_part (truncate)`: they differ on negatives (INTEGER(-3.7)=-4, INTEGER-PART(-3.7)=-3). Proven byte-identical: `integer_sweep` 20/0 + 1 unit test. Non-claims: non-numeric args, out-of-range magnitudes, INTEGER and INTEGER-PART interchangeable.\n\n## [0.7.10]\n- **GNURUST.INTRINSIC.MOD-REM.1 — FUNCTION MOD/REM (PATCH).** New `intrinsic::intrinsic_mod / intrinsic_rem` for integers: MOD takes the DIVISOR sign (MOD(-17,5)=3), REM the DIVIDEND sign (REM(-17,5)=-2). Proven byte-identical across the full sign matrix: `modrem_sweep` 20/0 + 1 unit test. Non-claims: non-integer operands, MOD/REM by zero, MOD and REM interchangeable.\n\n## [0.7.9]\n- **GNURUST.ACCEPT.DISPLAY.1 — DISPLAY/ACCEPT byte effects (PATCH).** New `accept_display::display_line / accept_field` reproduce DISPLAY (operands CONCATENATED, no separator, + one newline) and ACCEPT FROM CONSOLE (line moved into the field, padded/truncated). Proven byte-identical: `accept_display_sweep` 7/0 + 2 unit tests. Non-claims: DISPLAY of signed/V/edited numeric (reformats), DISPLAY UPON/NO ADVANCING, ACCEPT FROM DATE/TIME/environment.\n\n## [0.7.8]\n- **GNURUST.INTRINSIC.NUMVAL.1 — FUNCTION NUMVAL (PATCH).** New `intrinsic::intrinsic_numval` parses the narrow NUMVAL form (leading/trailing spaces, leading/trailing sign, CR/DB = negative, decimal point) to the same value as GnuCOBOL. Proven byte-identical: `numval_sweep` 14/0 + 1 unit test. Non-claims: NUMVAL-C currency/thousands, locale comma-decimal, national/UTF-8, malformed-input error semantics.\n\n## [0.7.7]\n- **GNURUST.INTRINSIC.LENGTH.1 — FUNCTION LENGTH (PATCH).** New `intrinsic::intrinsic_length` returns the storage byte length of an elementary field (X(n)=n, numeric digit count, COMP-3 packed bytes, binary width), == GnuCOBOL FUNCTION LENGTH. The first implemented intrinsic, split out of the GNURUST.INTRINSIC.ATLAS.1 map. Proven byte-identical: `length_sweep` 12/0 + 1 unit test. Non-claims: group/table/reference-modified LENGTH, LENGTH OF variant, national/UTF-8.\n\n## [0.7.6]\n- **GNURUST.STRING.UNSTRING.1 — STRING/UNSTRING byte effects (PATCH).** New `string_ops::string_into / unstring` reproduce STRING (DELIMITED BY SIZE/literal, 1-based POINTER, tail preserved, ON OVERFLOW partial writes kept) and UNSTRING (DELIMITED BY literal, COUNT IN = source length before truncation, DELIMITER IN = space at source exhaustion, TALLYING IN, 1-based POINTER, leftover = overflow). Proven byte-identical: `string_unstring_sweep` 7/0 + 7 unit tests. Non-claims: Procedure flow, national/UTF-8, multi-delimiter, locale, business parsing.\n\n## [0.7.5]\n- **GNURUST.INSPECT.1 — INSPECT byte effects (PATCH).** New `inspect::inspect_tallying/inspect_replacing/inspect_converting` reproduce INSPECT TALLYING (ALL/LEADING/CHARACTERS), REPLACING (ALL/LEADING/FIRST), and CONVERTING, with BEFORE/AFTER INITIAL regions. Scanning is left-to-right NON-OVERLAPPING (AAAAAA FOR ALL AA = 3), matching is exact bytes (no case folding). Proven byte-identical: `inspect_sweep` 12/0 + 4 unit tests. Non-claims: Procedure flow, locale/casefold, regex, national/UTF-8, multi-clause, business validation.\n\n## [0.7.4]\n- **GNURUST.INITIALIZE.1 — INITIALIZE receiver byte effects (PATCH).** New `initialize::initialize_record` reproduces a plain `INITIALIZE <record>`: elementary items get category defaults (X spaces, 9/S9 DISPLAY '0' digits, COMP-3 packed zero with sign nibble C signed / F unsigned, binary zero), while FILLER is preserved, a REDEFINES redefiner is skipped, every OCCURS element is initialized, and a VALUE clause is NOT restored. Proven byte-identical: `initialize_sweep` 6/0 + 3 unit tests. Non-claims: Procedure Division flow, REPLACING/TO VALUE, edited/JUSTIFIED, ODO active count, active REDEFINES view.\n\n## [0.7.3]\n- **GNURUST.FILE.SEQUENTIAL.1 — sequential file READ record bytes + status (PATCH).** New `file_seq::read_sequential` replays OPEN INPUT / READ NEXT / AT END for ORGANIZATION RECORD SEQUENTIAL (fixed binary: a short final record LEAKS the prior buffer tail; initial buffer is low-values) and LINE SEQUENTIAL (short line space-padded; a too-long line is chunked across reads with status 06; trailing newline yields no empty record), with file status 00/06/10. Proven byte-identical: `seqfile_sweep` 10/0 + 6 unit tests. Non-claims: indexed/relative/VSAM, WRITE/REWRITE, OPEN I-O/EXTEND, status beyond 00/06/10, locking, Procedure Division flow.\n\n## [0.7.2]\n- **GNURUST.REMAINDER.1 \u2014 DIVIDE REMAINDER receiving-field bytes (PATCH).** `arith::cob_divide_remainder` returns the quotient AND remainder receiver bytes for `DIVIDE a BY/INTO b GIVING q REMAINDER r`. The remainder is the COBOL definition \u2014 dividend \u2212 (quotient-as-stored \u00d7 divisor) \u2014 so it depends on the quotient receiver's scale/truncation; sign follows the dividend. DISPLAY/COMP-3, signed/unsigned, scaled/unscaled, BY/INTO. Proven byte-identical: `remainder_sweep` 768/0 + 6 unit tests. Non-claims: ON SIZE ERROR control flow, divide-by-zero (fail-closed), COMPUTE, Procedure Division, float, binary/edited receivers, business correctness.\n\n## [0.7.1]
- **GNURUST.19 — DIVIDE receiving-field bytes (PATCH, the first one).** `arith::cob_divide` writes
  `recv := lhs/rhs` byte-identical to `cobc` for `DIVIDE a BY b GIVING c` / `a INTO b GIVING c` over
  DISPLAY/COMP-3 operands+receivers, signed/scaled/narrowing, truncate + ROUNDED — proven `divide_sweep`
  736/0. `Op::Divide` + `cob_divide` + `ArithError::DivideByZero` are all **additive** (both enums are
  `#[non_exhaustive]` since 0.7.0) → **semver-patch**, the first patch demonstrating the future-proofing.
  Non-claims: divide-by-zero/`ON SIZE ERROR` (fail closed), `REMAINDER`, `COMPUTE`, procedure flow, float,
  binary/edited receivers, other rounding modes, business correctness.

## [0.7.0]

### GNURUST.18 — `pic`: COMP-6 unsigned packed-decimal storage + MOVE
- `Usage::Comp6` admitted: `build_field` → `{type=PACKED, NO_SIGN_NIBBLE, size=ceil(digits/2)}` matching
  `cobc` (`{0x12,n,0,0x0100}`; PIC sweep 432/0, +16 COMP-6). Two digits/byte, **no sign nibble**,
  unsigned. `cob_move` DISPLAY↔COMP-6 matches libcob (comp6_sweep 98/0); `Decimal::from_packed` decodes
  it. **Signed `S9(n)` COMP-6 is NOT this court** — GnuCOBOL warns and converts it to COMP-3.
- Non-claims: signed COMP-6, COMP-6 arithmetic, malformed bytes, dialect portability (strict
  cobol85/2002 reject it), pre-3.2. `release_scope = GnuCOBOL 3.2 only`.
- **Breaking (semver-minor):** `pic::Usage` gains `Comp6` **and is now `#[non_exhaustive]`** (so future
  usage variants stay additive). Downstream exhaustive `match`es on `Usage` need a wildcard arm.
- **API future-proofing (folded into this one breaking release): every growth-prone public type is now
  `#[non_exhaustive]`** so future courts are **patches, not minors**. Enums (variant growth): `Usage`,
  `arith::Op` (so GNURUST.19 DIVIDE → `Op::Divide` is a patch), `arith::Round`, `init::Val`,
  `cond::CondLit`, `cond::CondValue`. Output structs (field growth): `layout::Laid`, `pic::PicField`,
  `edited::EditedDecode`, `copybook::{Provenance, Expanded}`, `init::ValueItem`. User-constructed value
  types (`FieldAttr`, `Decimal`, `layout::{Item, Odo}`) are intentionally left exhaustive. **Intent: 0.7
  is the last breaking minor for a long time — accumulate courts as 0.7.x patches.**

## [0.6.3]
- **GNURUST.17 — cp500 EBCDIC zoned-decimal numeric DISPLAY decode.** `Decimal::from_ebcdic_zoned`
  decodes raw mainframe zoned-decimal bytes: cp500 translate (`GNURUST.15`) + the `cob_get_sign_ebcdic`
  overpunch sign (final byte `'A'..'I'`=+, `'J'..'R'`=−, `'{'`/`'}'`=±0, `'0'..'9'`=unsigned; raw zones
  `0xC`/`0xD`/`0xF`). Proven vs `cobc -fsign=EBCDIC` (ebcdic_num_sweep 120/0). Non-claims: cp037, edited
  numeric under cp500, binary/packed via this path, mixed encodings. Additive method → patch.

## [0.6.2]
- `layout::lay_out` now sizes **edited DISPLAY fields** via `edited::edited_size` (composing
  `GNURUST.4` + `GNURUST.16`), so a record containing an edited picture lays out instead of failing
  closed. The edited field's *decode* remains the `edited` court (not `pic`). Additive → patch.

## [0.6.1]
- **GNURUST.16b — edited-picture financial decorations (decode-only).** `decode_edited` now admits the
  `16b` subset (`$` currency fixed/floating, `*` check-protection, `CR`/`DB` trailing sign, `B` blank,
  `0` zero, `/` slash insertions) on top of `16a`. Decode is now **slot-based** (picture-position-aware)
  — required so a literal inserted `0` is recognised as a fixed char, not a value digit. Proven vs `cobc`
  MOVE→edited→DISPLAY (edited_sweep 92/0); 5M-run fuzz clean. No API change (additive behavior) → patch.

## [0.6.0]

### GNURUST.16 — `edited`: edited-picture DECODE (16a subset, decode-only)
- New `edited` module: `decode_edited(pic, bytes) -> EditedDecode` recovers an edited DISPLAY field's
  `numeric_value` + presentation `raw_text` for the admitted **16a** subset (`Z 9 , . - +`, `(n)`).
  Proven by moving values into edited fields with `cobc` and decoding the displayed bytes back
  (edited_sweep 50/50); 6M-run fuzz clean. `edited_size(pic)` gives the field width.
- Decode-only: numeric→edited formatting (16c), the financial decorations `$ * CR DB B 0 /` (16b),
  report semantics, locale/currency, EBCDIC edited, edited arithmetic, and edited `VALUE` are
  non-claims. Corrupt bytes / wrong width / out-of-subset symbols fail closed. Additive → minor.

## [0.5.0]

### GNURUST.15 — `ebcdic`: cp500 EBCDIC decode boundary for DISPLAY-class fields
- New `ebcdic` module: `decode_display(CodePage::Cp500, bytes) -> String` decodes raw EBCDIC
  alphanumeric DISPLAY bytes to text via the **cp500** table. The 256-byte table is byte-for-byte the
  one the admitted oracle's `cob_load_collation` produces for its shipped `ebcdic500_ascii8bit.ttbl`
  (sweep 256/256); decode is a pure per-byte lookup, so the full-range match is a complete proof.
- Oracle-faithful: GnuCOBOL 3.2 ships **cp500**, not cp037, so cp500 is admitted and **cp037 is
  deferred** (a code page is admitted only when its table comes from the oracle).
- Non-claims (fail closed): other code pages, **numeric EBCDIC zoned sign** processing, national/DBCS,
  collation, mixed/auto-detected encodings, and **binary/packed conversion** (raw bytes pass through
  untouched — EBCDIC is not a record-wide convert-everything). Additive (new module) → semver-minor.

## [0.4.1]
- Re-export `COB_TYPE_NUMERIC_BINARY` + binary flag constants (`COB_FLAG_BINARY_SWAP`/`REAL_BINARY`/
  `BINARY_TRUNC`) so downstream crates can dispatch binary decode. No semantic change.

## [0.4.0]

### GNURUST.14 — `pic`/`cob_move`: COMP / COMP-5 / COMP-X binary storage + MOVE
- Binary numeric **read fidelity**. `build_field` admits `USAGE COMP`/`BINARY`/`COMP-5`/`COMP-X` →
  `{type=BINARY, digits, scale, flags, size}` matching `cobc` (PIC sweep 416/0): COMP = big-endian +
  truncate (`SWAP|TRUNC`), COMP-X = big-endian full-range (`SWAP`, **tight** size), COMP-5 = native
  full-range (`REAL_BINARY`); sizes `1-2-4-8` for COMP/COMP-5, tight for COMP-X.
- `cob_move` now handles DISPLAY↔binary (and PACKED↔binary): move-result bytes match libcob —
  endianness, COMP digit-truncation, COMP-X/COMP-5 byte-masking, two's-complement signs (binary MOVE
  sweep 546/0). `Decimal::from_binary` added for the reconciliation read path. 10M move-fuzz clean.
- Non-claims: binary arithmetic, SYNCHRONIZED, host-portable endian, COMP-6, float — fail closed.
- **Breaking (semver-minor, pre-1.0):** `pic::Usage` gains `Comp`/`Comp5`/`CompX` variants (can break
  exhaustive `match`es). Published companion crates pin `^0.3`, so they are unaffected.

## [0.3.3]

### GNURUST.13 — `arith`: packed ADD/SUBTRACT (cob_add_bcd)
- `cob_arith` now seals `ADD`/`SUBTRACT` into a **PACKED (COMP-3) receiving field** — libcob's
  separate `cob_add_bcd` path, deferred in GNURUST.7. The receiving-field **bytes** match
  `cob_add`/`cob_sub` for DISPLAY/COMP-3 operands, signed/unsigned, scale differences, truncation and
  ROUNDED (nearest-away), odd/even digits, carry, and overflow. Sweep total=5400 PASS=5400 FAIL=0
  (receiver × operand ∈ {DISPLAY,COMP-3}²); 8M fuzz runs clean.
- Diagnosed two oracle facts: (1) GNURUST.7's deferral was an `opt` artifact — `cob_add_bcd` rounds
  only with the explicit mode bit (`ADD ROUNDED` = `0x21`), not `COB_STORE_ROUND` alone; (2) a
  **negative** result that truncates to **zero magnitude** keeps the negative sign (`-0`) on the
  packed path, unlike the DISPLAY path (`+0`). DIVIDE, SIZE ERROR, other rounding modes, bignum, and
  float remain non-claims. The `ArithError::PackedAddSubDeferred` variant is retained for API
  stability but no longer produced.

## [0.3.2]

### GNURUST.12 — `cond`: SET condition-name TO TRUE
- `gnucobol_rs::set_88_true(attr, size, condition)` (and in-place `apply_set_88_true`) constructs the
  canonical parent bytes GnuCOBOL writes for `SET condition-name TO TRUE`, matching `cobc`: the
  **first** `VALUE` entry is chosen (a literal as-is, or a `THRU` range's **lower bound**) and encoded
  into the parent (alnum space-padded; numeric zoned/packed via the sealed `cob_move`). The output
  always satisfies `eval_88` (round-trip self-check). Sweep total=52 PASS=52 FAIL=0; 6M fuzz clean.
  **TRUE-only** — `SET ... TO FALSE` / the `FALSE` clause, condition expressions, Procedure-Division
  execution, and collating-sensitive ranges are non-claims (fail closed). New `set_88_true`/
  `apply_set_88_true`/`ConditionSetError` (purely additive). Internally, the numeric value encoder is
  now shared between `value_image` and the SET constructor (value sweep unchanged at 392/0).

## [0.3.1]

### GNURUST.11 — `cond`: LEVEL-88 condition-name predicate
- `gnucobol_rs::eval_88(attr, bytes, condition)` evaluates whether a LEVEL-88 condition name is true
  for a parent field's current bytes, matching `cobc`: alphanumeric parents compare against the
  literal **space-padded to the parent length** (incl. `THRU` ranges, byte-wise); numeric DISPLAY/
  COMP-3 parents compare by **numeric value** (scale/sign-aware, `THRU` inclusive); single/multiple
  values and ranges. Sweep total=103 PASS=103 FAIL=0; 6M fuzz runs clean. **Predicate only** —
  `SET condition-name`, the `FALSE` clause, condition expressions, Procedure Division execution, and
  collating-sequence-sensitive ranges are non-claims (fail closed). New `cond` module + `Condition`/
  `CondLit`/`CondValue`/`ConditionError` types (purely additive).

## [0.3.0]

### GNURUST.10 — `layout`: OCCURS DEPENDING ON physical-max
- `lay_out` now admits a single, trailing `OCCURS min TO max TIMES DEPENDING ON <ctrl>` (elementary
  or group) as a **physical maximum-layout fact**: the item contributes `max` occurrences, and the
  record total is proven byte-identical to GnuCOBOL's physical allocation `b_REC[size]` (sweep
  `records=30 PASS=30 FAIL=0`). The **active/logical occurrence count is a non-claim**; runtime
  validation, sliding, VALUE-under-ODO, REDEFINES+ODO, multiple/nested ODO, ODO-not-last, and
  `max <= min` fail closed. Layout fuzz with ODO rules: 0 crashes.
- **Breaking (semver-minor, pre-1.0):** `layout::Item` gains a `pub odo: Option<Odo>` field, and a
  new `layout::Odo { min, max, depending_on }` type is exported. Construct `Item` with `odo: None`
  for non-ODO items. (Published companion crates pin `^0.2`, so they are unaffected.)

## [0.2.6]

### GNURUST.9 — `pic`: PIC P-scaling
- `build_field` now admits the `P` scaling symbol, producing the same `(type, digits, scale, size)`
  as `cobc`: **trailing P** (`999PPP`) -> `digits = 9s+P, scale = -P`; **leading P** (`PPP999`) ->
  `digits = 9s, scale = 9s+P`; storage `size` is always the stored `9`s (COMP-3 `n/2+1`, even though
  `digits` carries the P). Sweep PASS=288 FAIL=0. `V`+`P`, P at both ends, and P-only fail closed;
  VALUE/MOVE on a P field is deferred (`GNURUST.VALUE-P.0`) and fails closed (no panic). 6M PIC +
  3M VALUE fuzz runs clean.

## [0.2.5]

### GNURUST.8 — `init`: initial record image from VALUE clauses
- `gnucobol_rs::value_image(items)` computes the WORKING-STORAGE bytes a flat `01` record holds at
  program start, proven byte-identical to `cobc`-initialized storage: alphanumeric `VALUE` (left-
  justified, space-padded), numeric DISPLAY `VALUE` (zoned + overpunch sign), COMP-3 `VALUE` (packed
  via the sealed `cob_move`), and the type-correct defaults — unvalued DISPLAY numeric → `'0'`,
  unvalued alnum → spaces, **unvalued COMP-3 → canonical packed zero** (sign nibble `0x0C`/`0x0F`,
  not raw `0x00`). Sweep PASS=392 FAIL=0; 3M fuzz runs clean. OCCURS/REDEFINES+VALUE, edited/`P`
  PICs, non-fitting literals, and no-VALUE records fail closed.

## [0.2.4]

### GNURUST.7 — `arith`: decimal arithmetic (ADD/SUBTRACT/MULTIPLY)
- `gnucobol_rs::cob_arith` computes `a := a (op) b` in **pure-Rust integer decimal** (i128, zero
  deps, no float), proven byte-identical to libcob `cob_add`/`cob_sub`/`cob_mul`: ADD/SUBTRACT with
  a DISPLAY receiving field + MULTIPLY (DISPLAY/COMP-3), truncation and ROUNDED (nearest-away),
  cross-scale, all sign combos, negative-zero-on-overflow. Sweep PASS=1800 FAIL=0; 8M fuzz runs
  clean. ADD/SUBTRACT into a PACKED field (libcob's separate `cob_add_bcd` path), DIVIDE, the other
  rounding modes, ON SIZE ERROR, and >38-digit (bignum) inputs fail closed (deferred).

## [0.2.3]

### GNURUST.6 — `copybook`: COPY ... REPLACING (whole-text-word)
- `copybook::expand` now applies `COPY name REPLACING ==old== BY ==new== ….` at GnuCOBOL's
  **text-word** granularity (not string substitution): `==AA==` does not touch `AA-X`/`KEEP-AA`,
  the `:tag:` idiom works, multiple pairs apply per word, and a nested copy's brought-in text is
  penetrated by the outer REPLACING (after the nested copy's own) without altering nested operands.
  Proven against `cobc -P` (sweep `programs=7 PASS=7 FAIL=0`). Non-pseudo-text forms
  (`LEADING`/`TRAILING`, identifier operands, unterminated `==`, the `REPLACE` directive) fail
  closed. Fuzz: 4M runs, 0 crashes.

## [0.2.2]

### GNURUST.5 — `copybook`: COPY copybook expansion
- `gnucobol_rs::copybook::expand` splices `COPY <name>.` copybooks into the source — recursively,
  with cycle detection, depth/size limits, and a per-line **provenance map** — proven to match the
  GnuCOBOL preprocessor (`cobc -P`) at text-word granularity (sweep `programs=3 PASS=3 FAIL=0`).
  `COPY ... REPLACING` (whole-text-word replacement) is rejected as a deferred court (`GNURUST.6`);
  recursive/missing/over-deep/over-large copies fail closed. Fuzz: 3M runs, 0 crashes.

## [0.2.1]

### GNURUST.4 — `layout`: DATA DIVISION record layout
- `gnucobol_rs::layout::lay_out` assigns each record item its byte offset and one-occurrence size
  — level-numbered nested groups, fixed `OCCURS n TIMES`, `REDEFINES` overlay, `FILLER` — proven to
  match the GnuCOBOL compiler's own record layout (differential sweep vs `cobc` `records=6 PASS=32
  FAIL=0`; cross-checked by runtime `LENGTH OF`). `OCCURS DEPENDING ON`, `SYNCHRONIZED`, and a
  `REDEFINES` larger than its target fail closed. Fuzz: 5M runs, 0 crashes.
- Documentation refresh gate strengthened: every sealed campaign (per receipt) must now be
  referenced in the README and all load-bearing docs, or the gate fails (anti-staleness).

## [0.2.0]

### GNURUST.3 — `pic`: PICTURE → field model
- `gnucobol_rs::pic::build_field` parses the sealed PIC subset (`9 X A S V`, repeats,
  `SIGN [LEADING|TRAILING] [SEPARATE]`, `USAGE DISPLAY`/`COMP-3`) into `{type, digits, scale,
  flags, size}`, proven **byte-identical to the GnuCOBOL compiler's own field-attribute
  computation** (differential sweep PASS=192 FAIL=0 vs `cobc`-emitted `cob_field_attr` + size;
  cross-checked by runtime `LENGTH OF`).
- Fails closed (typed `PicError`) on the `P` scaling symbol (deferred), edited pictures, other
  usages, and malformed/oversized pictures. Fuzz: 5M runs, 0 crashes after fixing an OOM on giant
  repeats (now streamed, O(1) memory, resource-bounded — `GNURUST.DOS.0`).

## [0.1.1]
- Flagship crate published under the project name **`gnucobol-rs`** (lib `gnucobol_rs`); the
  internal decimal-court receipt id is `RECEIPT-GNURUST-DECIMAL-1`. No semantic change.

## [0.1.0]

### GNURUST.1 — admitted oracle + receipt harness
- Admitted GnuCOBOL 3.2 from pinned source (`research/gnucobol-3.2.tar.lz`), built `cobc` +
  `libcob` 3.2.0 (with Berkeley DB 5.3 INDEXED I/O) into a gitignored lab prefix.
- `cobc-oracle-rs`: build/run `cobc -x` fixtures and capture deterministic JSON receipts
  (source/stdout/stderr/exit + sha256, oracle + platform identity).
- Project doctrine: claim boundary, porting method, derivation/license boundary.

### GNURUST.2 — `gnucobol-rs` byte court
- Faithful LGPL-3.0+ port of GnuCOBOL packed-decimal (COMP-3), zoned, and display numeric
  byte semantics and the `MOVE` conversions between them, proven byte-identical against the
  built `libcob` runtime-library oracle. Kani reduced-surface proof + detached fuzz harness.
