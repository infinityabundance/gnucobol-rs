<!--
DO NOT EDIT BY HAND. Generated by lab/docs/generate.py from docs-src/compatibility-taxonomy.model.json
+ machine evidence (crates/gnucobol-rs/Cargo.toml, reports/claim-ladder.json, reports/casefiles/*,
reports/receipts/*). Regenerate: python3 lab/docs/generate.py. Check: python3 lab/docs/generate.py check.
Evidence authority: the claim-ladder + generated casefiles. Legacy source preserved (lossless) under research/legacyreports/docs/compatibility-taxonomy.md (sha256 68dddeef8681375e443df66ee4658a83471406e89925d4269b13e0d660c3ab43).
-->

# Compatibility taxonomy — the stack of separately admitted courts

> _Generated document (TRUST.4.DOCS). Machine authority: `reports/claim-ladder.json` + `reports/casefiles/`. Legacy source preserved losslessly under `research/legacyreports/docs/compatibility-taxonomy.md`._

> **`gnucobol-rs` treats COBOL compatibility as a stack of separately admitted courts: bytes,
> moves, initialization, comparison, formatting, source expansion, runtime lifecycle, files,
> reports, and diagnostics. No lower layer is allowed to imply a higher layer.**

This is the spine of the project. Each layer is proven against the oracle independently, ships
its own receipt, and carries its own non-claims. A green test in a lower court is never quoted
as evidence for a higher one.

| # | Court | Question it answers | Status |
|---|-------|---------------------|--------|
| 0a | **Field model** | does `PIC`+`USAGE` → `{type,digits,scale,flags,size}` match `cobc`? (`P`, COMP/COMP-5/COMP-X) | **sealed** — `pic` (`GNURUST.3`, `GNURUST.9`, `GNURUST.14`) |
| 0c | **Code page** | do raw EBCDIC DISPLAY bytes decode to the oracle's text under a named table? | **sealed** — `ebcdic` cp500 (`GNURUST.15`); cp037/numeric-zoned/DBCS fail closed |
| 0e | **EBCDIC numeric** | do raw cp500 zoned-decimal numeric bytes decode to the oracle's value? | **sealed** — `Decimal::from_ebcdic_zoned` (`GNURUST.17`); cp037/edited-numeric/mixed fail closed |
| 0f | **COMP-6** | does unsigned packed COMP-6 storage + MOVE match the oracle? | **sealed** — `Usage::Comp6` (`GNURUST.18`); signed COMP-6 / arithmetic / dialect fail closed |
| 0d | **Edited decode** | do edited DISPLAY field bytes decode to the oracle's value + text? | **sealed** — `edited` 16a (`GNURUST.16`); `$ * CR DB B 0 /` + numeric→edited fail closed |
| 0b | **Record layout** | do item offsets / group sizes / `OCCURS` / `REDEFINES` match `cobc`? (ODO physical-max) | **sealed** — `layout` (`GNURUST.4`, `GNURUST.10`) |
| 1 | **Storage parity** | does a field hold the same bytes? | **sealed** (`GNURUST.2`) |
| 2 | **Move parity** | do bytes after `MOVE src→dst` match? | **sealed** (`GNURUST.2`) |
| 2c | **Arithmetic parity** | do `ADD`/`SUBTRACT`/`MULTIPLY` result bytes match `cob_add`/`cob_mul`? | **sealed** — `arith` (`GNURUST.7` + packed add-sub `GNURUST.13`); DIVIDE / other modes future |
| 3 | **Initialization parity** | `VALUE` → initial record bytes | **sealed** — `init` (`GNURUST.8`); ODO/REDEFINES-VALUE, figuratives beyond ZERO/SPACE future |
| 4 | Comparison parity | LEVEL-88 predicate (`GNURUST.11`) + `SET TO TRUE` (`GNURUST.12`) **sealed**; `IF a < b`, `SEARCH ALL`, collation future | partial |
| 5 | Display/output parity | `DISPLAY`, edited pictures, Report Writer | future |
| 6 | Source/preprocess parity | `COPY` splice (`GNURUST.5`) + `REPLACING` (`GNURUST.6`) **sealed**; source-format/`REPLACE`-directive future | partial |
| 7 | Runtime-lifecycle parity | `CALL`/`CANCEL`, storage lifetimes, global state | future |
| 8 | File-behavior parity | sequential / line-seq / relative / indexed, status codes | future |
| 9 | Diagnostic parity | message class + phase + source span | future |

## The one rule that prevents overclaiming

A pass in court *N* says nothing about court *N+1*. Concretely, today:

- storage/move parity (1–2) does **not** imply comparison (4), display/edited (5), or file (8)
  parity;
- a `DISPLAY` stdout match would **not** imply storage parity — see `docs/runtime-doctrine.md`
  ("Display vs storage").

The full enumerated future courts and their explicit non-claims live in
[`future-risk-register.md`](future-risk-register.md); the machine-readable list of every
current non-claim lives in [`../reports/negative-claims.md`](../reports/negative-claims.md).

| 0g | **DIVIDE** | do DIVIDE GIVING receiver bytes match the oracle? | **sealed** — sequential REWRITE updates the current record in place (others unchanged) matching the oracle? | **sealed** — `file_seq` (`GNURUST.FILE.REWRITE.1`); LINE SEQ REWRITE, length change, DELETE, indexed fail closed |\n| sequential WRITE file bytes match the oracle (RECORD SEQ full padded / LINE SEQ trailing-space-stripped + newline)?sequential WRITE file bytes match the oracle (RECORD SEQ full padded / LINE SEQ trailing-space-stripped + newline)? | **sealed** — `file_seq` (`GNURUST.FILE.WRITE.1`); configured line modes, variable-length, WRITE ADVANCING, REWRITE fail closed |\n| the FILTER read-loop (per-record IF gates accumulation; numeric vs alphanumeric compare) yields the oracle's WORKING-STORAGE? | **sealed** — `file_flow_slice` (`GNURUST.FILE.FILTER.SLICE.1`); compound conditions, signed filter, per-record mutation fail closed |\n| the canonical read-loop (OPEN/PERFORM-UNTIL-EOF/READ/accumulate) EXECUTED yields the oracle's WORKING-STORAGE?the canonical read-loop (OPEN/PERFORM-UNTIL-EOF/READ/accumulate) EXECUTED yields the oracle's WORKING-STORAGE? | **sealed** — `file_flow_slice` (`GNURUST.FILE.FLOW.SLICE.1`); indexed/relative, per-record logic, WRITE-in-loop, multi-file fail closed |\n| a PERFORM loop (TIMES/UNTIL/VARYING over numeric counters) EXECUTED yields the oracle's storage?a PERFORM loop (TIMES/UNTIL/VARYING over numeric counters) EXECUTED yields the oracle's storage? | **sealed** — `perform_slice` (`GNURUST.PERFORM.SLICE.1`); signed/packed counters, body SIZE ERROR, non-ADD, THRU, nesting fail closed |\n| a NUMERIC IF/EVALUATE (compare 9(n) values, numeric MOVE branches) EXECUTED yields the oracle's storage? | **sealed** — `if_numeric` (`GNURUST.IF.NUMERIC.SLICE.1`); signed/V numerics, field-to-field MOVE, compound conditions fail closed |\n| an IF/EVALUATE fragment (alphanumeric, MOVE branches) EXECUTED over a record yields the oracle's storage?an IF/EVALUATE fragment (alphanumeric, MOVE branches) EXECUTED over a record yields the oracle's storage?an IF/EVALUATE fragment (alphanumeric, MOVE branches) EXECUTED over a record yields the oracle's storage? | **sealed** — `if_eval` (`GNURUST.IF.EVALUATE.SLICE.1`); numeric/compound/class conditions, 88-level, non-MOVE, nesting fail closed |\n| arithmetic overflow truncation (low-order digits kept) + the size-error condition match the oracle?arithmetic overflow truncation (low-order digits kept) + the size-error condition match the oracle?arithmetic overflow truncation (low-order digits kept) + the size-error condition match the oracle? | **sealed** — `size_error` (`GNURUST.SIZE.ERROR.1`); the arithmetic value, ROUNDED, MOVE overflow, float fail closed |\n| DISPLAY of a signed (+/- prefix) / V-scaled (inserted '.') numeric matches the oracle?DISPLAY of a signed (+/- prefix) / V-scaled (inserted '.') numeric matches the oracle? | **sealed** — `accept_display` (`GNURUST.ACCEPT.DISPLAY.2`); numeric-edited, BLANK WHEN ZERO, JUSTIFIED fail closed |\n| DISPLAY emitted text (operands concatenated + newline) and ACCEPT field bytes (line move, pad/truncate) match the oracle?DISPLAY emitted text (operands concatenated + newline) and ACCEPT field bytes (line move, pad/truncate) match the oracle? | **sealed** — `accept_display` (`GNURUST.ACCEPT.DISPLAY.1`); signed/V/edited DISPLAY, UPON/NO ADVANCING, ACCEPT FROM DATE fail closed |\n| date-conversion intrinsics (INTEGER-OF-DATE etc., proleptic Gregorian 1601-01-01=1) match the oracle? | **sealed** — `intrinsic` (`GNURUST.INTRINSIC.DATE.1`); invalid dates, CURRENT-DATE, Y2K windowing fail closed |\n| FUNCTION ORD (1-based, ORD('A')=66) / CHAR (inverse) match the oracle?FUNCTION ORD (1-based, ORD('A')=66) / CHAR (inverse) match the oracle? | **sealed** — `intrinsic` (`GNURUST.INTRINSIC.ORD-CHAR.1`); non-default collation, national/UTF-8, out-of-range fail closed |\n| FUNCTION UPPER-CASE/LOWER-CASE (ASCII fold) / REVERSE (byte reversal) match the oracle?FUNCTION UPPER-CASE/LOWER-CASE (ASCII fold) / REVERSE (byte reversal) match the oracle? | **sealed** — `intrinsic` (`GNURUST.INTRINSIC.CASE.1`); locale/national folding, multibyte REVERSE fail closed |\n| FUNCTION INTEGER (floor) / INTEGER-PART (truncate) match the oracle (differ on negatives)?FUNCTION INTEGER (floor) / INTEGER-PART (truncate) match the oracle (differ on negatives)? | **sealed** — `intrinsic` (`GNURUST.INTRINSIC.INTEGER.1`); non-numeric, out-of-range, dialect fail closed |\n| FUNCTION MOD (divisor sign) / REM (dividend sign) integer results match the oracle across the sign matrix?FUNCTION MOD (divisor sign) / REM (dividend sign) integer results match the oracle across the sign matrix? | **sealed** — `intrinsic` (`GNURUST.INTRINSIC.MOD-REM.1`); non-integer, divide-by-zero, dialect fail closed |\n| FUNCTION NUMVAL-C (currency string: strip $ + thousands commas) parses to the oracle value? | **sealed** — `intrinsic` (`GNURUST.INTRINSIC.NUMVAL-C.1`); non-default currency, locale comma-decimal, national fail closed |\n| FUNCTION NUMVAL(narrow numeric string) parses to the oracle valueFUNCTION NUMVAL(narrow numeric string) parses to the oracle valueFUNCTION NUMVAL(narrow numeric string) parses to the oracle valueFUNCTION NUMVAL(narrow numeric string) parses to the oracle value (trailing sign / CR / DB = negative)? | **sealed** — `intrinsic` (`GNURUST.INTRINSIC.NUMVAL.1`); NUMVAL-C currency, locale comma-decimal, national/UTF-8 fail closed |\n| FUNCTION LENGTH(elementary field) = the storage byte length matches the oracleFUNCTION LENGTH(elementary field) = the storage byte length matches the oracle (COMP-3 packed bytes, binary width)? | **sealed** — `intrinsic` (`GNURUST.INTRINSIC.LENGTH.1`); group/table, reference-modified, national/UTF-8 fail closed |\n| STRING/UNSTRING receiver + pointer/count/delimiter/tally bytes match the oracleSTRING/UNSTRING receiver + pointer/count/delimiter/tally bytes match the oracle (1-based pointer, tail preserved, COUNT IN pre-truncation)? | **sealed** — `string_ops` (`GNURUST.STRING.UNSTRING.1`); national/UTF-8, multi-delimiter, locale, business parsing fail closed |\n| INSPECT TALLYING/REPLACING/CONVERTING target + tally bytes match the oracleINSPECT TALLYING/REPLACING/CONVERTING target + tally bytes match the oracle (non-overlapping, exact-byte, BEFORE/AFTER regions)? | **sealed** — `inspect` (`GNURUST.INSPECT.1`); locale/casefold, regex, national/UTF-8, multi-clause fail closed |\n| a plain `INITIALIZE <record>`'s changed/preserved receiver bytes match the oraclea plain `INITIALIZE <record>`'s changed/preserved receiver bytes match the oracle (category defaults; FILLER/redefiner preserved; VALUE not restored)? | **sealed** — `initialize` (`GNURUST.INITIALIZE.1`); Procedure flow, REPLACING/TO VALUE, edited receivers fail closed |\n| sequential `READ NEXT` record bytes + file status (00/06/10) for RECORD/LINE SEQUENTIAL match the oracle?sequential `READ NEXT` record bytes + file status (00/06/10) for RECORD/LINE SEQUENTIAL match the oracle? | **sealed** — `file_seq` (`GNURUST.FILE.SEQUENTIAL.1`); indexed/relative/VSAM, WRITE/REWRITE, OPEN I-O fail closed |\n| `arith::cob_divide` (`GNURUST.19`)`arith::cob_divide` (`GNURUST.19`) + `cob_divide_remainder` (`GNURUST.REMAINDER.1`, quotient+remainder bytes); divide-by-zero/ON SIZE ERROR/COMPUTE/float/binary-receiver fail closed`arith::cob_divide` (`GNURUST.19`); divide-by-zero/REMAINDER/COMPUTE/float/binary-receiver fail closed |

