<!--
DO NOT EDIT BY HAND. Generated by xtask docs from docs-src/claim-boundary.model.json
+ machine evidence (crates/gnucobol-rs/Cargo.toml, reports/claim-ladder.json, reports/casefiles/*,
reports/receipts/*). Regenerate: cargo run -p xtask -- docs generate. Check: cargo run -p xtask -- docs check.
Evidence authority: the claim-ladder + generated casefiles. Legacy source preserved (lossless) under research/legacyreports/docs/claim-boundary.md (sha256 f943e2c8c70d8ac73ef399e2c2fc760f75dd9bf5982be130cc2bfb509f7f6656).
-->

# Claim boundary

> _Generated document (TRUST.4.DOCS). Machine authority: `reports/claim-ladder.json` + `reports/casefiles/`. Legacy source preserved losslessly under `research/legacyreports/docs/claim-boundary.md`._

The first law of `gnucobol-rs`: **every claim names its boundary, and every positive claim
is matched by an equally loud non-claim.** A compatibility surface that does not say what it
excludes is a lie of omission.

## What `gnucobol-rs` is

- A **compatibility court**: a set of memory-safe Rust ports of narrow GnuCOBOL semantics,
  each proven against a locally built GnuCOBOL 3.2 oracle.
- **Oracle-first**: "correct" = "byte/verdict-identical to the built `cobc`/`libcob`", never
  "matches our reading of the COBOL standard".
- **Receipt-bearing**: each sealed slice ships an admission receipt (pinned hashes, oracle
  identity, build command, env) and a parity receipt (`PASS=n FAIL=0` + classified rows).

## What `gnucobol-rs` is NOT (yet, or by design)

- **Not a GnuCOBOL replacement.** It reproduces isolated semantics, not the product.
- **Not a full COBOL compiler.** An independently written front-end (`src/frontend.rs`, the `cobrun` binary — the author's from-scratch claim; strict clean-room provenance is not independently verifiable, see [methodology](docs/methodology/parser-front-end-provenance.md)) now PARSES and EXECUTES a small, explicit SUBSET of whole programs to byte-identical `cobc` stdout (`GNURUST.FRONTEND.1`, `cobol_frontend_sweep` 10/0 + `cobol_run_sweep` 3/0) -- so 'cannot parse/run a program' is no longer flatly true. But this is an INTERPRETER over the sealed runtime, NOT a native-code compiler: no machine-code/object emission is claimed, and only the listed subset (WORKING-STORAGE 01 elementary items + MOVE/ADD/SUBTRACT/MULTIPLY/DIVIDE/DISPLAY/STOP RUN) runs -- everything else fails closed.
-**Not a `libcob` replacement.** A handful of runtime primitives are ported, not the runtime.
- **Not decimal arithmetic.** `ADD`/`SUBTRACT`/`MULTIPLY`/`DIVIDE`/`ROUNDED` are GMP-backed
  in upstream and are explicitly deferred to a future, separately sealed campaign.
- **Not a "better" or ergonomic COBOL library.** A divergence from the oracle that happens to
  be "nicer" is a **bug**, not an improvement.
- **Not a definition of COBOL truth.** The ISO/ANSI standard and IBM/Micro Focus behavior are
  not authorities here; the built GnuCOBOL oracle is.
- **Not a diagnostics match.** Compiler message wording is not reproduced.

## Sealed slices

**`GNURUST.2` — decimal byte semantics.** For admitted PICs, the Rust model reproduces GnuCOBOL's
exact bytes for packed-decimal (COMP-3 / PACKED-DECIMAL), zoned-with-sign, and display numeric
fields, and the `MOVE` conversions between them, under `LC_ALL=C.UTF-8` on a little-endian ASCII
host. (`reports/RECEIPT-GNURUST-DECIMAL-1.md`.)

**`GNURUST.3` — PIC → field model.** `pic::build_field` parses the sealed PIC subset
(`9 X A S V`, repeats, `SIGN` clause, `USAGE DISPLAY`/`COMP-3`) into the same
`{type, digits, scale, flags, size}` the compiler emits. (`reports/RECEIPT-GNURUST-PIC-3.md`.)

**`GNURUST.4` — record layout.** `layout::lay_out` assigns each DATA DIVISION item its byte offset
and size within an `01` record (nested groups, fixed `OCCURS`, `REDEFINES` overlay, `FILLER`),
matching the compiler's record layout. (`reports/RECEIPT-GNURUST-LAYOUT-4.md`.)

**`GNURUST.5` — COPY expansion.** `copybook::expand` splices `COPY <name>.` copybooks (recursively,
cycle-detected, provenance-mapped) matching the GnuCOBOL preprocessor (`cobc -P`) at text-word
granularity. (`reports/RECEIPT-GNURUST-COPY-5.md`.)

**`GNURUST.6` — COPY ... REPLACING.** `copybook::expand` applies `COPY name REPLACING ==p== BY ==q==`
at whole-text-word granularity, composing across nesting, matching `cobc -P`. Non-pseudo-text forms
(`LEADING`/`TRAILING`, identifier operands) fail closed. (`reports/RECEIPT-GNURUST-REPLACING-6.md`.)

**`GNURUST.7` — decimal arithmetic.** `cob_arith` computes ADD/SUBTRACT (DISPLAY) and MULTIPLY (DISPLAY/COMP-3) with truncation/ROUNDED in pure-Rust integer decimal, matching libcob `cob_add`/`cob_mul`. ADD/SUBTRACT into PACKED (cob_add_bcd), DIVIDE, other rounding modes, ON SIZE ERROR, and >38-digit inputs fail closed. (`reports/RECEIPT-GNURUST-ARITH-7.md`.)

**`GNURUST.8` — VALUE initial image.** `value_image` computes an `01` record's initial WORKING-STORAGE bytes from `VALUE` clauses (alphanumeric left-justified/space-padded, numeric DISPLAY zoned+overpunch, COMP-3 packed; unvalued DISPLAY→`'0'`, alnum→spaces, COMP-3→packed zero), matching `cobc`. OCCURS/REDEFINES+VALUE, edited/`P` PICs, non-fitting literals fail closed. (`reports/RECEIPT-GNURUST-VALUE-8.md`.)

**`GNURUST.9` — PIC `P` scaling.** `build_field` admits `P`: trailing `digits=9s+P, scale=-P`; leading `digits=9s, scale=9s+P`; `size` = stored `9`s — matching `cobc`. `V`+`P`/both-ends/P-only and VALUE/MOVE on a P field fail closed. (`reports/RECEIPT-GNURUST-PIC-SCALING-9.md`.)

**`GNURUST.10` — ODO physical-max layout.** `lay_out` admits a single trailing `OCCURS min TO max DEPENDING ON` as its **physical maximum** (max occurrences), matching `cobc`'s `b_REC[size]` allocation. The active/logical count, sliding, and runtime meaning are **non-claims**; multiple/nested ODO, ODO-not-last, REDEFINES+ODO, and `max<=min` fail closed. (`reports/RECEIPT-GNURUST-ODO-10.md`.)

**`GNURUST.CLASS.1` — class conditions.** `is_numeric`/`is_alphabetic[-upper/-lower]` decide `IF X IS NUMERIC/ALPHABETIC` over an alphanumeric field (byte-faithful to cobc). **`GNURUST.11` — LEVEL-88 predicate.** `eval_88` proves whether a condition name is true for a parent field's current bytes — alphanumeric (space-padded compare, incl. ranges) and numeric DISPLAY/COMP-3 (value compare, inclusive ranges) — matching `cobc`. `SET`/`FALSE`/expressions/Procedure-Division execution and collating-sensitive ranges are non-claims. (`reports/RECEIPT-GNURUST-COND-11.md`.)

**`GNURUST.12B` — SET LEVEL-88 TO FALSE.** `set_88_false` writes the `WHEN SET TO FALSE IS` literal into the parent (byte-faithful to cobc). **`GNURUST.12` — SET LEVEL-88 TO TRUE.** `set_88_true` constructs the canonical parent bytes for `SET condition-name TO TRUE` (first VALUE / range lower bound, encoded), matching `cobc`; its output satisfies `eval_88`. `SET TO FALSE`/the FALSE clause/expressions/execution are non-claims. (`reports/RECEIPT-GNURUST-SET88-12.md`.)

**`GNURUST.13` — packed ADD/SUBTRACT.** `cob_arith` seals ADD/SUBTRACT into a PACKED receiver (libcob's `cob_add_bcd` path), matching the receiving-field **bytes** for DISPLAY/COMP-3 operands, scales, truncate/ROUNDED, carry, overflow, and negative-zero-on-truncation. DIVIDE / SIZE ERROR / other rounding modes / bignum are non-claims. (`reports/RECEIPT-GNURUST-ADDBCD-13.md`.)

**`GNURUST.14` — binary storage + MOVE.** `build_field` admits COMP/BINARY/COMP-5/COMP-X (type/digits/scale/flags/size vs `cobc`), and `cob_move` handles DISPLAY↔binary (endian, truncate/mask, two's-complement), with `Decimal::from_binary` for decode. Binary arithmetic, SYNC, host-portable endian are non-claims. (`reports/RECEIPT-GNURUST-BINARY-14.md`.)

**`GNURUST.15` — EBCDIC code-page boundary.** `ebcdic::decode_display` decodes raw EBCDIC alphanumeric DISPLAY bytes to text under the admitted **cp500** table (byte-for-byte the oracle's `cob_load_collation` output, 256/256). cp037, numeric EBCDIC zoned sign, national/DBCS, and binary/packed conversion are non-claims. (`reports/RECEIPT-GNURUST-EBCDIC-15.md`.)

**`GNURUST.16C` — numeric→edited encode.** `edited::encode_edited` formats a numeric value into edited DISPLAY bytes byte-faithful to cobc (the inverse direction of decode); `edited_encode_sweep` 92/0. **`GNURUST.16` — edited-picture decode (16a).** `edited::decode_edited` recovers an edited DISPLAY field's value + presentation text for the `Z 9 , . - +` subset (proven vs `cobc` MOVE→edited→DISPLAY, 50/50). Numeric→edited formatting, `$ * CR DB B 0 /` (16b), reports, locale, EBCDIC edited, and edited arithmetic are non-claims. (`reports/RECEIPT-GNURUST-EDITED-16.md`.)

**`GNURUST.17` — cp500 EBCDIC zoned numeric.** `Decimal::from_ebcdic_zoned` decodes raw cp500 zoned-decimal bytes (cp500 translate + cob_get_sign_ebcdic sign), proven vs `cobc -fsign=EBCDIC` (120/0). cp037, edited-numeric under cp500, and binary/packed via this path are non-claims. (`reports/RECEIPT-GNURUST-EBCDICNUM-17.md`.)

**`GNURUST.18` — COMP-6.** `Usage::Comp6` admits unsigned packed-decimal (PACKED+NO_SIGN_NIBBLE, size ceil(n/2)) storage + DISPLAY↔COMP-6 MOVE, proven vs cobc/cob_move (432/0, 98/0). Signed COMP-6 (→COMP-3), arithmetic, malformed bytes, dialect portability are non-claims. (`reports/RECEIPT-GNURUST-COMP6-18.md`.)

**Non-claims:** no arithmetic; no edited pictures (`PIC $,9.99`); no `P` scaling; no binary
(`COMP`/`COMP-5`)/float/`COMP-6` parity beyond what a receipt records; no EBCDIC-host sign mode
(the ASCII overpunch path is the sealed one); no `OCCURS DEPENDING ON`/`SYNCHRONIZED`; no
`REDEFINES` larger than its target; no file I/O. See `reports/negative-claims.md`.

## Determinism: pinned or classified

Every confounder is either pinned to a reproducible value or classified out of the claim:

| Confounder | Disposition |
|------------|-------------|
| Locale | pinned `LC_ALL=C.UTF-8` |
| Host endianness / charset | classified: little-endian ASCII host (`COB_EBCDIC_MACHINE` off) |
| GnuCOBOL build flags | recorded in the admission receipt as "which upstream" |
| `cobc`/`libcob` version | pinned 3.2.0; recorded with sha256 |

No silent third state: a surface is sealed, pinned, or classified — never blurred.


**`GNURUST.FILE.REWRITE.1` — sequential REWRITE.** `file_seq::rewrite_records` overwrites the current record in place (same length), other records unchanged. LINE SEQ REWRITE, length-changing rewrites, DELETE, and indexed/relative fail closed. **`GNURUST.FILE.WRITE.1` — sequential WRITE.** `file_seq::write_sequential` -- RECORD SEQ full padded record (no delimiter); LINE SEQ trailing-space-stripped + newline. **`GNURUST.FILEIO.LINESEQ.1` — line-sequential WRITE config matrix.** `fileio::lineseq_write`/`lineseq_size` seal the `COB_LS_FIXED`/`COB_LS_NULLS`/`COB_LS_VALIDATE` modes byte-identically (default validate rejects any record byte < 0x20 with status 71; the source `IS_BAD_CHAR` exclusions are dead in the 3.2 GA binary). **`GNURUST.FILEIO.LINESEQ.2` — line-sequential READ.** `fileio::lineseq_read` seals READ NEXT byte-identically (
 fold, lone- data, status 09 bad-char, COB_LS_NULLS decode, COB_LS_SPLIT 06-vs-04 truncation, EOF 10); the `IS_BAD_CHAR` exclusions (BS/TAB/FF/SI/ESC) are LIVE on read but dead on write. **`GNURUST.FILEIO.SEQ.1` — RECORD SEQUENTIAL.** `fileio::sequential_write`/`sequential_read`/`set_sequential_variable_length` seal fixed records (full area, short-final leaks the prior tail) and the variable-length `COB_VARSEQ_FORMAT` length prefix (BE16+NUL / BE32 / native LE32 / BE16) byte-identically. **`GNURUST.FILEIO.RELATIVE.1` — RELATIVE.** `fileio::relative_write`/`relative_read`/`relative_read_next`/`relative_rewrite`/`relative_delete`/`relative_start` seal the slot array (8-byte native-endian size header + `record_max` data; `0`=empty/deleted) with keyed status 00/22/23/24 and a DELETE that tombstones the header but keeps the data. **`GNURUST.FILEIO.VERB.1` -- verb preconditions.** `fileio::cob_write`/`cob_read`/`cob_read_next`/`cob_rewrite`/`cob_delete`/`cob_start` seal the open/access-mode FILE STATUS a verb returns before dispatch (WRITE to INPUT 48, READ from OUTPUT 47, REWRITE/DELETE outside I-O 49, sequential REWRITE/DELETE without a prior READ 43, sequential READ past EOF 46). **`GNURUST.FILEIO.SORT.1` -- SORT comparison.** `fileio::sort_cmps`/`cob_file_sort_compare`/`cob_file_sort_init_key` seal the record order a SORT produces -- per-key byte ranges, ASCENDING/DESCENDING mixed, a stable insertion-order tiebreak -- verified against a real SORT GIVING file; numeric keys (via NUMCMP.1) and the sort engine's tempfile merge are non-claims. **`GNURUST.FILEIO.SYS.1` -- CBL_* file/dir routines.** `fileio::cob_sys_delete_file`/`cob_sys_copy_file`/`cob_sys_rename_file`/`cob_sys_create_dir`/`cob_sys_delete_dir`/`cob_sys_change_dir`/`cob_sys_get_current_dir` seal the `CBL_DELETE_FILE`/`COPY_FILE`/`RENAME_FILE`/`CREATE_DIR`/`DELETE_DIR`/`CHANGE_DIR`/`GET_CURRENT_DIR` RETURN-CODE (0/35/128/129/-1) against the oracle (plus the `CBL_OPEN`/`READ`/`WRITE`/`CLOSE_FILE` handle round-trip); `CBL_CHECK_FILE_EXIST` date formatting is a non-claim. **`GNURUST.FILEIO.OPEN.1` -- file runtime.** A `CobFile` ties the sealed handlers together: `fileio::cob_open`/`cob_close`/`cob_pre_open`/`cob_unlock`/`cob_commit`/`cob_rollback`/`cob_delete_file` produce the open/close FILE STATUS (41 already-open, 42 not-open, 38 closed-with-lock, 35 missing input / 05 OPTIONAL, 31 bad filename) and flush the written file image, with WRITE/READ NEXT dispatching by organization -- verified end-to-end against an OPEN OUTPUT/WRITE/READ/CLOSE program; indexed/BDB open and real locking are non-claims. **`GNURUST.FILEIO.MAPPING.1` -- filename resolution.** `fileio::cob_chk_file_env`/`cob_chk_file_mapping` seal how an `OPEN` resolves a COBOL ASSIGN name to a path: a bare name is looked up via `DD_<name>`/`dd_<name>`/`<name>` env vars (`.`->`_` mangled), an absolute env value is used as-is, else `COB_FILE_PATH` is prefixed; a leading `.`/`-`/digit is not mapped -- verified by where the oracle actually creates the file. The complex multi-element path mapping is a non-claim. **`GNURUST.FILEIO.SORTENGINE.1` -- the SORT engine.** `fileio::CobSort` is a 1:1 port of libcob's in-memory 4-queue natural-merge sort (`cob_file_sort_init`/`submit`/`process`/`retrieve`/`cob_sort_queues`/`cob_new_item`/`using`/`giving`): submitting records then draining yields byte-identical output order to the oracle's `SORT ... GIVING`, with the `unique` insertion-counter tiebreak making it a STABLE permutation -- verified against a 16-record duplicate-laden dataset. The temp-file spill path (above `COB_SORT_MEMORY`) and the EXTFH variants are non-claims. **`GNURUST.FILEIO.INDEXED.1` -- INDEXED organization.** `fileio::IndexedStore` ports the COBOL-observable behaviour of the `indexed_*` handlers over a primary RECORD KEY (indexed_sweep 18/0): WRITE rejects a duplicate primary key with 22, READ by key returns the record or 23, READ NEXT is strictly key-ordered (10 at end), START positions by an =/</<=/>/>= condition, REWRITE of an absent key is 21, DELETE is 00/23. The record/file locking sub-layer (`lock_record`/`unlock_record`/`test_record_lock`/`lock_file`/`unlock_file`) ports the NOWAIT grant/deny status contract (a record locked by another open is 51, a file 61). The on-disk index format, alternate keys, and cross-process BDB locking are non-claims. **`GNURUST.FILEIO.MULTI-RECORD-FD.1` -- multiple 01-level record descriptions beneath one FD.** `frontend` parses EVERY level-01 record declared under an FD/SD (source order), gives each its own layout while sharing ONE record area (GnuCOBOL union, oracle-verified), pre-resolves record->file ownership, and resolves `WRITE`/`REWRITE`/`RELEASE` of ANY declared record (the CCVS85 `WRITE DUMMY-RECORD AFTER ADVANCING` report pattern was the original blocker); `AFTER/BEFORE ADVANCING n` line control (n x LF, 0 -> CR) + the CLOSE final-LF (`flag_needs_nl`) now mirror the oracle's disk bytes byte-for-byte (multirecord_fd_sweep 7/0; IC101A's full 3752-byte report is byte-identical to the oracle). **`GNURUST.MLIO.GENERATE.1` -- native XML/JSON GENERATE.** `mlio::cob_xml_generate_new`/`cob_json_generate_new` serialize a `cob_ml_tree` to byte-identical `XML GENERATE` / `JSON GENERATE` output as the oracle (which uses libxml2 + json-c) -- *without linking those libraries* (ml_generate_sweep 2/0). XML PARSE is now native too (`GNURUST.MLIO.PARSE.1`, ml_parse_sweep 2/0): GnuCOBOL 3.2 XML PARSE is an unimplemented stub, so the port reproduces its observable START-OF-DOCUMENT->END-OF-INPUT->END-OF-DOCUMENT event walk natively -- mlio.c is now 100% ported with no libxml2/cJSON linked. JSON PARSE does not exist in GnuCOBOL 3.2. **`GNURUST.COMMON.BOUNDCHECK.1`** -- the runtime EC-BOUND diagnostic text for an out-of-bounds subscript / reference modification / OCCURS DEPENDING ON is reproduced byte-identically (common.c `cob_check_subscript`/`cob_check_ref_mod`/`cob_check_odo`), verified IDENTICAL across BOTH admitted oracles GnuCOBOL 3.1.2 + 3.2 (bounds_check_sweep 6/0) -- a version-stable claim. **`GNURUST.COMMON.NUMCHECK.1`** likewise reproduces the not-numeric runtime diagnostic `'<name>' (Type: <type>) not numeric: '<escaped>'` (common.c `explain_field_type` + `cob_check_numeric`), also verified across both oracles (numeric_check_sweep 2/0). **`GNURUST.COMMON.CBL.1`** reproduces the CBL_ logic/bit/case builtins (CBL_AND/OR/XOR/NOR/IMP/NIMP/EQ/NOT/TOUPPER/TOLOWER) byte-identically, also across both oracles (cbl_logic_sweep 2/0). The `libcob:` prefix framing and the abort are non-claims. **`GNURUST.SCREENIO.INIT.1`** -- the SCREEN SECTION init/teardown framing + a positioned literal/FROM DISPLAY are reproduced BYTE-IDENTICALLY to GnuCOBOL's ncurses terminal output natively (screenio_sweep 1/0, no ncurses linked) for the admitted terminal (TERM=xterm, ncurses 6.6); the cursor-cost model is now sealed too (`GNURUST.SCREENIO.DISPLAY.2`, screenio_grid_sweep 70/0 over a LINE x COLUMN grid -- ncurses's space-fill/HPA/VPA/CUP choice reproduced byte-for-byte); multi-field DISPLAY (the inter-field mvcur from a non-home origin) is now sealed too (`GNURUST.SCREENIO.DISPLAY.3`, screenio_multi_sweep 21/0, non-overlapping layouts); the monochrome display attributes (HIGHLIGHT/LOWLIGHT/UNDERLINE/BLINK/REVERSE-VIDEO) are now sealed too (`GNURUST.SCREENIO.ATTR.1`, screenio_attr_sweep 10/0, the SGR on/off wrap); the SINGLE-field COLOUR DISPLAY whole-screen repaint is now sealed too (`GNURUST.SCREENIO.COLOR.1`, screenio_color_sweep 11/0 -- the COBOL->curses colour bit-reversal + the default-pair fallback + the wclear/TransformLine repaint, no ncurses linked); the SINGLE-field NUMERIC-EDITED display positioning is now sealed too (`GNURUST.SCREENIO.NUMEDIT.1`, screenio_numedit_sweep 14/0 -- leading-blank skip + all-blank-field park, composing the sealed edit engine; it also surfaced + fixed a complete-zero-suppression blanking bug in `edited::encode_edited`); the SINGLE-field alphanumeric ACCEPT input (width<=6) is now sealed too (`GNURUST.SCREENIO.ACCEPT.1`, screenio_accept_sweep 12/0 -- prompt + reposition + echo + field-full), and its OVERFLOW (typing past the field width -- BEL + overwrite) (`GNURUST.SCREENIO.ACCEPT.2`, screenio_accept2_sweep 11/0) -- both DIFFERENTIALLY verified byte-identical against BOTH GnuCOBOL 3.2 and 3.1.2; the TWO-DISPLAY same-row refresh line-diff (the ncurses doupdate/TransformLine clr_eol erase for overlapping same-row layouts) is now sealed too (`GNURUST.SCREENIO.LINEDIFF.1`, screenio_linediff_sweep 19/0, differentially verified against BOTH 3.2 and 3.1.2 -- the reposition cost search + clr_eol + the cursor-uncertainty CUP); THREE-or-more-display + distant-survivor + multi-ROW line-diffs, the LINE==1 colour edge, the `*`-fill edit rule, the width>=7 `rep` prompt + edit-key ACCEPT, and any other terminal remain the declared non-claims (terminfo-dependent).Variable-length records, WRITE ADVANCING, REWRITE, and indexed/relative fail closed. **`GNURUST.FILE.FILTER.SLICE.1` — filter read-loop.** `file_flow_slice::eval_filter_loop` gates each record's accumulation by a numeric or alphanumeric `IF`. Compound conditions, signed filters, per-record mutation, and EVALUATE classifiers fail closed. **`GNURUST.FILE.FLOW.SLICE.1` — read-loop slice.** `file_flow_slice::eval_read_loop` EXECUTES the OPEN/PERFORM-UNTIL-EOF/READ/accumulate loop to oracle-identical WORKING-STORAGE (each record once). Indexed/relative, per-record logic, WRITE-in-loop, multi-file, and signed accumulators fail closed. **`GNURUST.SEARCH.TABLE.1` — SEARCH / SEARCH ALL.** `search::search_serial` (forward-from-index) / `search_all` (binary on ascending key) return the 1-based landing index. DESCENDING/multi keys, non-numeric keys, unsorted SEARCH ALL, and the WHEN/AT END bodies fail closed. **`GNURUST.TABLE.PERFORM.SLICE.1` — table PERFORM VARYING.** `table_slice::eval_table_loop` runs `PERFORM VARYING I … TABLE(I)` over a **1-based** OCCURS table. Multi-dim/nested OCCURS, OCCURS DEPENDING ON, out-of-bounds, INDEXED/SEARCH, and signed elements fail closed. **`GNURUST.PERFORM.SLICE.1` — PERFORM execution slice.** `perform_slice::eval_perform` EXECUTES `PERFORM TIMES/UNTIL/VARYING` over numeric counters to oracle-identical storage (test-before; VARYING ends one past the limit). Signed/packed counters, body SIZE ERROR, non-ADD bodies, THRU/paragraph, and nesting fail closed. **`GNURUST.IF.NUMERIC.SLICE.1` — numeric IF/EVALUATE slice.** `if_numeric::eval_if_numeric / eval_evaluate_numeric` compare 9(n) VALUES (not bytes) and apply numeric MOVE branches. Signed/V numerics, field-to-field MOVE, SIZE ERROR, compound conditions, and range WHEN fail closed. **`GNURUST.IF.EVALUATE.SLICE.1` — first execution slice.** `if_eval::eval_if / eval_evaluate` EXECUTE a bounded alphanumeric `IF`/`EVALUATE` fragment to oracle-identical storage. Numeric comparison, compound/class conditions, 88-level, non-MOVE branches, and nested flow fail closed. **`GNURUST.SIZE.ERROR.1` — arithmetic SIZE ERROR.** `size_error::arith_size_error` -- overflow truncates the low-order digits (high dropped) and flags the condition (ON SIZE ERROR leaves the receiver unchanged). The arithmetic value, ROUNDED, and MOVE overflow fail closed. **`GNURUST.ACCEPT.DISPLAY.2` — DISPLAY of signed/V numeric.** `accept_display::display_numeric` -- signed `+`/`-` prefix (positive zero `+`) + `V` decimal point. Numeric-edited PICs, BLANK WHEN ZERO, and JUSTIFIED fail closed. **`GNURUST.ACCEPT.DISPLAY.1` — DISPLAY/ACCEPT.** `accept_display::display_line/accept_field` reproduce DISPLAY (operands concatenated + newline) and ACCEPT FROM CONSOLE (line move, pad/truncate). DISPLAY of signed/V/edited numerics, DISPLAY UPON/NO ADVANCING, and ACCEPT FROM DATE/TIME/environment fail closed. **`GNURUST.INTRINSIC.DATE.1` — date conversion.** `intrinsic::intrinsic_integer_of_date / date_of_integer / integer_of_day / day_of_integer` -- proleptic-Gregorian day numbers (1601-01-01 = 1). Invalid dates, CURRENT-DATE/WHEN-COMPILED, and Y2K windowing fail closed. **`GNURUST.INTRINSIC.ORD-CHAR.1` — FUNCTION ORD/CHAR.** `intrinsic::intrinsic_ord/intrinsic_char` -- 1-based ordinal (ORD('A')=66) and inverse. Non-default collating sequences, national/UTF-8, and out-of-range CHAR fail closed. **`GNURUST.INTRINSIC.CASE.1` — UPPER-CASE/LOWER-CASE/REVERSE.** `intrinsic::intrinsic_upper_case/lower_case/reverse` -- ASCII fold (non-alpha unchanged) + byte reversal. Locale/national folding and multibyte REVERSE fail closed. **`GNURUST.INTRINSIC.INTEGER.1` — FUNCTION INTEGER / INTEGER-PART.** `intrinsic::intrinsic_integer (floor) / intrinsic_integer_part (truncate)` differ on negatives. Non-numeric args, out-of-range magnitudes, and dialect variance fail closed. **`GNURUST.INTRINSIC.MOD-REM.1` — FUNCTION MOD/REM.** `intrinsic::intrinsic_mod/intrinsic_rem` for integers: MOD takes the divisor sign, REM the dividend sign (they DIFFER on negatives). Non-integer operands, divide-by-zero, and dialect variance fail closed. **`GNURUST.INTRINSIC.NUMVAL-C.1` — FUNCTION NUMVAL-C.** `intrinsic::intrinsic_numval_c` strips the default `$` and thousands commas then parses as NUMVAL. Non-default currency symbol, locale comma-decimal, and national/UTF-8 fail closed. **`GNURUST.INTRINSIC.NUMVAL.1` — FUNCTION NUMVAL.** `intrinsic::intrinsic_numval` parses the narrow NUMVAL form (sign, spaces, CR/DB = negative, decimal) to the GnuCOBOL value. NUMVAL-C currency, locale comma-decimal, national/UTF-8, and malformed-input error semantics fail closed. **`GNURUST.INTRINSIC.LENGTH.1` — FUNCTION LENGTH.** `intrinsic::intrinsic_length` returns the storage byte length of an elementary field (== GnuCOBOL), the same count as the sealed field model. Group/table, reference-modified, LENGTH OF, and national/UTF-8 length fail closed. **`GNURUST.STRING.UNSTRING.1` — STRING/UNSTRING.** `string_ops::string_into/unstring` reproduce STRING (1-based POINTER, tail preserved, ON OVERFLOW) + UNSTRING (COUNT IN = pre-truncation source length, DELIMITER IN = space at exhaustion, TALLYING). National/UTF-8, multi-delimiter, locale, and business parsing fail closed. **`GNURUST.ODO.1` — OCCURS DEPENDING ON.** `odo_used_length`/`odo_element` give a variable-length group's used length (`fixed_prefix + N*element_size`) and bounded `E(i)` access, fail-closed beyond the active count. **`GNURUST.SUBSCRIPT.1` — table subscripts.** `element_1d`/`element_2d` extract `TABLE(i)`/`TABLE(i,j)` (1-based, offset = sum (idx-1)*stride), byte-exact in bounds and fail-closed out of bounds. **`GNURUST.ROUND.1` — `ROUNDED MODE IS`.** `do_round` ports all eight rounding modes (nearest-away default, nearest-even, nearest-toward-zero, away-from-zero, toward-greater, toward-lesser, prohibited, truncation) on the `cob_decimal` store path, byte-exact vs `cobc`; the packed `cob_add_bcd` path is covered too (ROUND.2): it matches `cob_decimal` except NEAREST-EVEN, which the BCD path resolves away-from-zero. **`GNURUST.BIGNUM.1` — MULTIPLY beyond i128.** an overflowing product is carried as the exact 256-bit value, rounded and truncated to the receiver byte-for-byte (no fail-closed, no K>38 defer). **`GNURUST.INTPOW.1` — integer `**`.** `cob_s32_pow`/`cob_s64_pow` with libcob's exact edges (base==-1 -> 1, negative power -> 0, wrapping overflow). **`GNURUST.LOGICAL.1` — `B-AND`/`B-OR`/`B-XOR`/`B-NOT` + shifts.** bitwise over `|value| mod 2^64` (sign discarded), shift count mod 64. **`GNURUST.FLOAT.1` — float fields.** `COMP-1`/`COMP-2`/`FLOAT-DECIMAL-16/34` field bytes + decimal<->float conversions, both directions: GnuCOBOL TRUNCATES decimal->double TOWARD ZERO (GMP `mpf_get_d`), `COMP-1` then narrows round-to-nearest, and `FLOAT-DECIMAL` is BID-encoded — all byte-exact; float ARITHMETIC stays a non-claim. **`GNURUST.NUMCMP.1` — numeric comparison.** the -1/0/1 verdict of `cob_numeric_cmp` across DISPLAY/PACKED, differing scales (aligned), all signs (incl. -0) -- the first 1:1 port on the new pure-Rust GMP-mpz subset (`gmp::Mpz`) + `cob_decimal` layer. **`GNURUST.INDEX.1` — `USAGE INDEX` storage.** `index_store`/`set_index_to`/`set_index_up_by`/`set_index_down_by` give the 4-byte native-endian word an index item holds: the **occurrence number** (element-size independent, never an offset), `SET TO/UP BY/DOWN BY` as plain integer arithmetic (DOWN past zero stores the two’s-complement negative; cobc does not clamp). **`GNURUST.REFMOD.1` — reference modification.** `ref_mod`/`apply_ref_mod` extract or overwrite `field(start:length)` (1-based), byte-exact in bounds and fail-closed out of bounds. **`GNURUST.INSPECT.1` — INSPECT.** `inspect::inspect_tallying/replacing/converting` reproduce INSPECT TALLYING/REPLACING/CONVERTING byte effects + tally bytes, left-to-right non-overlapping, exact-byte matching, with BEFORE/AFTER INITIAL regions. Locale/casefold, regex, national/UTF-8, and multi-clause generalization fail closed. **`GNURUST.INITIALIZE.1` — INITIALIZE.** `initialize::initialize_record` reproduces a plain INITIALIZE: category defaults for elementary items (X spaces, 9/S9 '0' digits, COMP-3 sign nibble C/F, binary zero), FILLER preserved, REDEFINES redefiner skipped, every OCCURS element set, VALUE NOT restored. Procedure flow, REPLACING/TO VALUE, and edited receivers fail closed. **`GNURUST.FILE.SEQUENTIAL.1` — sequential READ.** `file_seq::read_sequential` reproduces OPEN INPUT / READ NEXT / AT END record bytes + file status (00/06/10) for RECORD SEQUENTIAL (a short final record leaks the prior buffer tail; initial low-values) and LINE SEQUENTIAL (a too-long line is chunked, status 06). Indexed/relative/VSAM, WRITE/REWRITE, and status beyond 00/06/10 fail closed. **`GNURUST.REMAINDER.1` — DIVIDE REMAINDER.** `arith::cob_divide_remainder` writes the quotient + `REMAINDER` receiver bytes as `cobc` for `DIVIDE a BY/INTO b GIVING q REMAINDER r` (the remainder is dividend — stored-quotient × divisor; sign follows the dividend; DISPLAY/COMP-3, scaled/unscaled). ON SIZE ERROR / divide-by-zero / COMPUTE / binary receivers fail closed. **`GNURUST.19` — DIVIDE.** `arith::cob_divide` writes `recv := lhs/rhs` byte-for-byte as `cobc` for `DIVIDE ... GIVING` (DISPLAY/COMP-3, truncate + ROUNDED), proven 736/0. Divide-by-zero, REMAINDER, COMPUTE, ON SIZE ERROR, float, and binary/edited receivers are non-claims. (`reports/RECEIPT-GNURUST-DIVIDE-19.md`.)

## The GnuCOBOL-testsuite courts (GNURUST.GNUCOBOL-TESTSUITE.{1,2,3}) and the harness courts

`GNURUST.GNUCOBOL-TESTSUITE.1` is the admitted GnuCOBOL 3.2 native Autotest suite custody + real-compiler baseline + invocation census; `GNURUST.GNUCOBOL-TESTSUITE.2` is the candidate execution through the native harness with `COBC=cobc-rs` and the mechanical no-delegation proof; `GNURUST.GNUCOBOL-TESTSUITE.3` is the differential classification with every test accounted. `GNURUST.GNUCOBOL-RUNTIME-MATH.1` is the math-subset classification (performance separately labeled). `GNURUST.METHODOLOGY.LIBCOB.1` / `GNURUST.METHODOLOGY.PARSER.1` record the runtime/parser provenance. `GNURUST.COBC-RS.ARGS.1` / `GNURUST.COBC-RS.LAUNCHER.1` / `GNURUST.COBC-RS.PARALLEL.1` are the cobc-rs driver courts (option policy, launcher manifest, concurrency). None of these is a conformance or parity claim; they are differential observations and boundary statements with preserved raw evidence.

## The external-corpus courts (GNURUST.CCVS85.2 / GNURUST.CCVS85.3 / GNURUST.CCVS85.4)

`GNURUST.CCVS85.2` is the NIST CCVS85 materialization + real-GnuCOBOL oracle baseline,
`GNURUST.CCVS85.3` is the `gnucobol-rs` (cobrun) execution baseline over the same units, and
`GNURUST.CCVS85.4` is the differential comparison + per-unit classification. All three are
**observation courts over the admitted historical CCVS85 v4.0 corpus** — none of them is a NIST
certification, none establishes COBOL-85 conformance, none claims `cobc` replacement, and none
converts unsupported or unexecuted units into passes. An oracle rejection is evidence only about
the pinned GnuCOBOL 3.2 build; a raw-output match is evidence only about the tested environment.

### Boundary-reduction courts (0.8.54+)

The boundary-reduction work added: GNURUST.GNUCOBOL-TESTSUITE.4, GNURUST.GNUCOBOL-TESTSUITE.BOUNDARY-REDUCTION.1, GNURUST.MODULE.REGISTRY.1, GNURUST.MODULE.CALL.1, GNURUST.MODULE.CANCEL.1, GNURUST.MODULE.SEARCH.1, GNURUST.MODULE.PARALLEL.1, GNURUST.COBC-RS.NATIVE-MODE-BOUNDARY.1, GNURUST.COBC-RS.POLICY-COMPLETE.1, GNURUST.GNUCOBOL-RUNTIME-MATH.2 -- the module lifecycle (GNURUST.MODULE.*), the native-mode/policy boundaries (GNURUST.COBC-RS.*), the re-measured suite court (GNURUST.GNUCOBOL-TESTSUITE.4), the before/after transition ledger (GNURUST.GNUCOBOL-TESTSUITE.BOUNDARY-REDUCTION.1) and the re-measured math campaign (GNURUST.GNUCOBOL-RUNTIME-MATH.2).

