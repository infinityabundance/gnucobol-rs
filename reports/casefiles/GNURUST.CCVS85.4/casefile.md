<!-- DO NOT EDIT BY HAND. Generated from casefile.json by kobold-courts.
     Evidence of record: casefile.json. Portable attestations: sarif.json, intoto-statement.json, dsse-envelope.json. -->
# Forensic case file — GNURUST.CCVS85.4 (court-casefile)

**Verdict: PASS** · reports/ccvs85/* (generated by bash lab/ccvs85/run-docker.sh from the committed corpus spine) · crate `gnucobol-rs` 0.8.51

- **Oracle:** pinned GnuCOBOL 3.2.0 vs gnucobol-rs cobrun (differential)
- **Byte domain(s):** comparison-results.json (per-unit oracle/candidate/comparison + final_classification), summary.json/.md, results.csv, failure-buckets.md, receipts, raw per-unit evidence
- **Replay:** `bash lab/ccvs85/run-docker.sh`
- **Authority:** STATUS.md · receipt_status: current

## Positive claims (4)
- for all 512 indexed units, the pinned-GnuCOBOL outcome is compared with the cobrun outcome on raw output, documented canonical output, generated files, exit status, and parsed CCVS85 verdict counts
- every unit finishes in exactly one of 23 classifications
- two fresh isolated-container runs produce byte-identical summaries (determinism)
- the gate check (corpus identity, 512-accounted, reconciliation, no-delegation, receipt freshness) passes

## Negative claims (8) — negative capability is the trust surface
- no NIST certification
- no full COBOL-85 conformance
- no full cobc replacement
- no native-code-generation comparison (cobrun interprets)
- matching output does not prove equivalence outside the tested environment
- an oracle rejection does not prove the source invalid under every implementation
- blocked units are never converted to passes
- lie prevented: a raw-output match on a NULL-output program is reported as exactly that (empty-bytes match), never as proof of feature equivalence

## Damage if overclaimed
reporting the court as a certification would give a 1993 validation corpus an authority it never had

> Generated forensic evidence (TRUST.4). The binding record is `casefile.json`; this `.md` is a rendering.
> Portable attestations: `sarif.json` (findings), `intoto-statement.json` (provenance), `dsse-envelope.json`.
