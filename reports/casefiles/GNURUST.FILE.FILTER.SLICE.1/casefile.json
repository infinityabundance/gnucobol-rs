{
  "authority": {
    "current_authority": "STATUS.md",
    "generated_by": "kobold-courts",
    "receipt_status": "current"
  },
  "byte_domains": [
    "read-loop with a per-record IF gating the accumulation -> resulting WORKING-STORAGE"
  ],
  "case_id": "GNURUST.FILE.FILTER.SLICE.1",
  "crate": "gnucobol-rs",
  "crate_version": "0.7.67",
  "damage_if_overclaimed": "using byte comparison on a numeric amount filter (or vice versa) selects the wrong record set, corrupting every conditional total/extract",
  "hash_chain": {
    "inputs_sha256": "b3d21c4f862e56f1354bb6116d61bb3c7849b0dc126e4f10a10bbd937f669639"
  },
  "inputs": {
    "claim_ladder_entry_sha256": "4d3290dc008e304c0cc04ca0f866821b6b275024d91196c169d61cf39f70ee88",
    "receipt_sha256": "9bfb26fdb14e075e14487d5a478f348759bc8957531e6f46a908a138144a6e0e"
  },
  "kind": "court-casefile",
  "lie_prevented": [
    "filtering is the same whether the field is numeric or text -- NO: a NUMERIC filter compares VALUES (R-AMT>=50 keeps 100 and 200) while an ALPHANUMERIC filter compares BYTES (where '5' > '10'), so applying the wrong comparison kind silently selects the wrong records"
  ],
  "negative_capability_ids": [
    "NEG.COMPOUND-CONDITIONS-AND-OR",
    "NEG.SIGNED-PACKED-NUMERIC-FILTER",
    "NEG.PER-RECORD-MOVE-TRANSFORM",
    "NEG.MULTI-BRANCH-EVALUATE-FILTER",
    "NEG.INDEXED-RELATIVE",
    "NEG.ALL-DIALECTS"
  ],
  "negative_claims": [
    "compound conditions (AND/OR)",
    "signed/packed numeric filter",
    "per-record MOVE/transform",
    "multi-branch EVALUATE filter",
    "indexed/relative",
    "all dialects",
    "lie prevented: filtering is the same whether the field is numeric or text -- NO: a NUMERIC filter compares VALUES (R-AMT>=50 keeps 100 and 200) while an ALPHANUMERIC filter compares BYTES (where '5' > '10'), so applying the wrong comparison kind silently selects the wrong records"
  ],
  "oracle": {
    "detail": "cobc filter read-loop (libcob/fileio.c + cobc control flow)",
    "dialect_profile_id": "gnucobol-3.2.0-default",
    "dialect_profile_sha256": "a321d7dfd4db4b492c1dfdaddc16eb876cf204d82630b8a54e5f6e01ab00c77c",
    "oracle_kind": "gnucobol-3.2-admitted",
    "upstream_court": "GNURUST.FILE.FILTER.SLICE.1"
  },
  "positive_claims": [
    "the SELECTIVE-accumulation read-loop -- the workhorse of COBOL batch -- executed to oracle-identical WORKING-STORAGE (verified 4/4). Each record's accumulation is gated by a per-record IF: a NUMERIC relation (compare the field's decoded unsigned value -- so R-AMT>=50 keeps 100/50/200, excludes 25/7) or an ALPHANUMERIC relation (space-padded byte compare in the ASCII collating sequence -- R-ST=A). Deepens GNURUST.FILE.FLOW.SLICE.1 with the per-record condition the read-loop court refused, distinguishing numeric (5<10) from alphanumeric ('5'>'10') comparison"
  ],
  "replay": {
    "command": "bash lab/oracle/file_filter_sweep.sh",
    "exit_code": 0
  },
  "results": {
    "fail": 0,
    "pass": 4,
    "total": 4,
    "verdict": "pass"
  },
  "schema": "kobold-forensic-casefile-v1"
}