{
  "authority": {
    "current_authority": "STATUS.md",
    "generated_by": "kobold-courts",
    "receipt_status": "current"
  },
  "byte_domains": [
    "OPEN + WRITE/READ/REWRITE/DELETE/START (RELATIVE) -> slot array bytes (8-byte LE header + record_max) + FILE STATUS (00/22/23/24/10)"
  ],
  "case_id": "GNURUST.FILEIO.RELATIVE.1",
  "crate": "gnucobol-rs",
  "crate_version": "0.7.57",
  "damage_if_overclaimed": "mis-sizing the slot header (width/endianness) or treating a deleted slot as absent vs zero-data shifts or corrupts every relative record's addressing",
  "hash_chain": {
    "inputs_sha256": "d992ca354fdfb7ba08106b4af65158b99f254fe3b57a0a21b41d4f964edad994"
  },
  "inputs": {
    "claim_ladder_entry_sha256": "ff2e2503bc1db43400da6c0ee45a3aa2ebcef12b72c6c740f47f395739f31036",
    "receipt_sha256": "8b168adfe02d79199f04dedcc8ed4fde04b7935e68206268a5a905b77f489c8c"
  },
  "kind": "court-casefile",
  "lie_prevented": [
    "a relative file is a packed array of records -- NO: each record is a fixed slot prefixed by an 8-byte native-endian length header, a DELETE only zeroes that header (the data bytes survive), an empty/deleted slot READs as status 23, and writing key N zero-fills the gap up to slot N-1"
  ],
  "negative_capability_ids": [
    "NEG.VARIABLE-LENGTH-RELATIVE-RECORDS",
    "NEG.THE-READ-FIRST-LAST-PREVIOUS-DIRECTIONS",
    "NEG.COB-READ-MASK-OPTION-BITS",
    "NEG.THE-SEQUENTIAL-ACCESS-RELATIVE-KEY-WRITE",
    "NEG.OPEN-EXTEND-FILE-SHARING-AND-RECORD-LOCK",
    "NEG.THE-EXTFH-EXTERNAL-FILE-HANDLER-PATH",
    "NEG.THE-ACTUAL-FD-LSEEK-READ-WRITE-SYSCALLS"
  ],
  "negative_claims": [
    "variable-length relative records",
    "the READ FIRST/LAST/PREVIOUS directions of read_next",
    "COB_READ_MASK option bits",
    "the SEQUENTIAL-access RELATIVE KEY write-back on append",
    "OPEN EXTEND / file-sharing and record locking",
    "the EXTFH external file handler path",
    "the actual fd lseek/read/write syscalls (declared OS boundary)",
    "lie prevented: a relative file is a packed array of records -- NO: each record is a fixed slot prefixed by an 8-byte native-endian length header, a DELETE only zeroes that header (the data bytes survive), an empty/deleted slot READs as status 23, and writing key N zero-fills the gap up to slot N-1"
  ],
  "oracle": {
    "detail": "cobc OPEN/WRITE/READ/REWRITE/DELETE RELATIVE RANDOM+SEQUENTIAL (libcob/fileio.c)",
    "dialect_profile_id": "gnucobol-3.2.0-default",
    "dialect_profile_sha256": "a321d7dfd4db4b492c1dfdaddc16eb876cf204d82630b8a54e5f6e01ab00c77c",
    "oracle_kind": "gnucobol-3.2-admitted",
    "upstream_court": "GNURUST.FILEIO.RELATIVE.1"
  },
  "positive_claims": [
    "a faithful port of libcob/fileio.c relative_write/relative_read/relative_read_next/relative_rewrite/relative_delete/relative_start, proven byte-identical to the admitted libcob (relative_sweep 4/0): an ORGANIZATION IS RELATIVE file is an array of fixed slots of relsize = sizeof(record->size)+record_max bytes, each = an 8-byte NATIVE-ENDIAN size header (>0 active, 0 empty/deleted) + record_max data",
    "a record at relative key N lives at slot (N-1). Keyed WRITE writes the slot (zero-filling gaps), status 22 on an occupied slot, 24 on key<1",
    "READ/REWRITE/DELETE address slot (key-1) -> 23 on an empty slot, 24 on key<1",
    "DELETE tombstones by zeroing the header and LEAVES the data",
    "sequential READ NEXT skips empty/deleted slots and is EOF (10) at end",
    "START positions by EQ/GE/GT/LE/LT/FIRST/LAST. Verified file bytes + per-op FILE STATUS + a READ NEXT scan"
  ],
  "replay": {
    "command": "bash lab/oracle/relative_sweep.sh",
    "exit_code": 0
  },
  "results": {
    "fail": 0,
    "pass": 4,
    "total": 4,
    "verdict": "pass"
  },
  "schema": "kobold-forensic-casefile-v1"
}