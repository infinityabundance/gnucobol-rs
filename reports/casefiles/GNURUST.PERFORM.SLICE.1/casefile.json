{
  "authority": {
    "current_authority": "STATUS.md",
    "generated_by": "kobold-courts",
    "receipt_status": "current"
  },
  "byte_domains": [
    "execute PERFORM loop over numeric counters -> resulting storage bytes"
  ],
  "case_id": "GNURUST.PERFORM.SLICE.1",
  "crate": "gnucobol-rs",
  "crate_version": "0.7.82",
  "damage_if_overclaimed": "assuming the control variable stops at the limit (or that the body always runs at least once) is an off-by-one in iteration counts and post-loop variable values",
  "hash_chain": {
    "inputs_sha256": "f8abfce117efcbbbb411957af9b23642c10fb3361653c758e77892ccbd758630"
  },
  "inputs": {
    "claim_ladder_entry_sha256": "fd3ca8edc744617f0791dfd048c66046b4be817c171e81b49e0856f2fc0a6300",
    "receipt_sha256": "e2676ffc0029bfd88f46afb0dd994a6363187942e5080fd994674bd3f49eaef0"
  },
  "kind": "court-casefile",
  "lie_prevented": [
    "PERFORM VARYING stops AT the limit -- NO: it tests BEFORE and the control variable ends ONE STEP PAST the limit (FROM 1 BY 1 UNTIL I>4 leaves I=5, not 4), and a TEST-BEFORE loop whose condition is already true runs the body ZERO times; off-by-one here corrupts every loop-bounded computation"
  ],
  "negative_capability_ids": [
    "NEG.SIGNED-PACKED-BINARY-COUNTERS",
    "NEG.NUMERIC-SIZE-ERROR-ON-THE-BODY",
    "NEG.NON-ADD-BODY-STATEMENTS",
    "NEG.COMPOUND-CLASS-CONDITIONS",
    "NEG.PERFORM-THRU-OUT-OF-LINE-PARAGRAPH",
    "NEG.WITH-TEST-AFTER",
    "NEG.NESTED-PERFORM-GO-TO",
    "NEG.ALL-DIALECTS"
  ],
  "negative_claims": [
    "signed/packed/binary counters",
    "numeric SIZE ERROR on the body",
    "non-ADD body statements",
    "compound/class conditions",
    "PERFORM THRU / out-of-line paragraph",
    "WITH TEST AFTER",
    "nested PERFORM/GO TO",
    "all dialects",
    "lie prevented: PERFORM VARYING stops AT the limit -- NO: it tests BEFORE and the control variable ends ONE STEP PAST the limit (FROM 1 BY 1 UNTIL I>4 leaves I=5, not 4), and a TEST-BEFORE loop whose condition is already true runs the body ZERO times; off-by-one here corrupts every loop-bounded computation"
  ],
  "oracle": {
    "detail": "cobc PERFORM (cobc/typeck.c + codegen.c, libcob)",
    "dialect_profile_id": "gnucobol-3.2.0-default",
    "dialect_profile_sha256": "a321d7dfd4db4b492c1dfdaddc16eb876cf204d82630b8a54e5f6e01ab00c77c",
    "oracle_kind": "gnucobol-3.2-admitted",
    "upstream_court": "GNURUST.PERFORM.SLICE.1"
  },
  "positive_claims": [
    "the second execution slice: a narrow interpreter EXECUTES a PERFORM loop over unsigned 9(n) counter fields and produces the same resulting STORAGE BYTES as cobc/libcob (verified 10/0). PERFORM n TIMES runs the body n times (n<=0 -> none)",
    "PERFORM UNTIL tests BEFORE each iteration (a satisfied condition runs the body zero times)",
    "PERFORM VARYING v FROM a BY b UNTIL sets v=a then test-before, running the body and adding b to v, so the control variable ends ONE STEP PAST the limit when the loop ran (FROM 2 BY 3 UNTIL I>10 ends I=11) or stays at a if never entered. Composes ADD + numeric comparison under the witnessed PROCEDURE.FLOW.ATLAS.1 loop semantics"
  ],
  "replay": {
    "command": "bash lab/oracle/perform_sweep.sh",
    "exit_code": 0
  },
  "results": {
    "fail": 0,
    "pass": 10,
    "total": 10,
    "verdict": "pass"
  },
  "schema": "kobold-forensic-casefile-v1"
}