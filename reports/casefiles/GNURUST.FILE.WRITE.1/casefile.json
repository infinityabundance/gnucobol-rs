{
  "authority": {
    "current_authority": "STATUS.md",
    "generated_by": "kobold-courts",
    "receipt_status": "current"
  },
  "byte_domains": [
    "OPEN OUTPUT + WRITE -> file bytes (RECORD SEQ full padded / LINE SEQ trailing-space-stripped + LF)"
  ],
  "case_id": "GNURUST.FILE.WRITE.1",
  "crate": "gnucobol-rs",
  "crate_version": "0.8.49",
  "damage_if_overclaimed": "assuming LINE SEQUENTIAL preserves trailing spaces (or that RECORD SEQUENTIAL adds a newline) corrupts every written record's framing",
  "hash_chain": {
    "inputs_sha256": "a87d76ab8b850cf6dfd0d71a3a2b9e25847d10a285d64f0d848388746440c1c8"
  },
  "inputs": {
    "claim_ladder_entry_sha256": "75831ecd232970bb28d8c3dbc1d64aa9b5390c44a3695ec96262950f4290bb4a",
    "receipt_sha256": "add08f9a6b4d26e647f6996a54796311a18d944d4e42bc603851316a4e584bfa"
  },
  "kind": "court-casefile",
  "lie_prevented": [
    "WRITE just dumps the record bytes -- NO: LINE SEQUENTIAL STRIPS trailing spaces and adds a newline (so an X(8) holding AB writes 3 bytes, and an all-spaces record writes just a newline), while RECORD SEQUENTIAL writes the FULL fixed padded record with no delimiter -- the two organizations produce different bytes for the same record"
  ],
  "negative_capability_ids": [
    "NEG.VARIABLE-LENGTH-RECORDS",
    "NEG.WRITE-ADVANCING-BEFORE-AFTER",
    "NEG.REWRITE",
    "NEG.INDEXED-RELATIVE-ORGANIZATIONS",
    "NEG.ALL-DIALECTS-COB-LS-FIXED-COB-LS-NULLS-L"
  ],
  "negative_claims": [
    "variable-length records",
    "WRITE ADVANCING/BEFORE/AFTER",
    "REWRITE",
    "indexed/relative organizations",
    "all dialects (COB_LS_FIXED/COB_LS_NULLS line modes now sealed by GNURUST.FILEIO.LINESEQ.1)",
    "lie prevented: WRITE just dumps the record bytes -- NO: LINE SEQUENTIAL STRIPS trailing spaces and adds a newline (so an X(8) holding AB writes 3 bytes, and an all-spaces record writes just a newline), while RECORD SEQUENTIAL writes the FULL fixed padded record with no delimiter -- the two organizations produce different bytes for the same record"
  ],
  "oracle": {
    "detail": "cobc OPEN OUTPUT/WRITE (libcob/fileio.c)",
    "dialect_profile_id": "gnucobol-3.2.0-default",
    "dialect_profile_sha256": "a321d7dfd4db4b492c1dfdaddc16eb876cf204d82630b8a54e5f6e01ab00c77c",
    "oracle_kind": "gnucobol-3.2-admitted",
    "upstream_court": "GNURUST.FILE.WRITE.1"
  },
  "positive_claims": [
    "the file bytes produced by OPEN OUTPUT + repeated WRITE over fixed records, matching cobc/libcob fileio.c (verified 2/0): RECORD SEQUENTIAL writes the full fixed record_len-wide record area (space-padded) with NO delimiter",
    "LINE SEQUENTIAL STRIPS trailing spaces and appends a newline (an all-spaces record writes just a newline). The WRITE side of GNURUST.FILE.SEQUENTIAL.1 (the READ court)"
  ],
  "replay": {
    "command": "bash lab/oracle/write_seq_sweep.sh",
    "exit_code": 0
  },
  "results": {
    "fail": 0,
    "pass": 2,
    "total": 2,
    "verdict": "pass"
  },
  "schema": "kobold-forensic-casefile-v1"
}