{
  "authority": {
    "current_authority": "STATUS.md",
    "generated_by": "kobold-courts",
    "receipt_status": "current"
  },
  "byte_domains": [
    "CALL USING parameter byte layout: BY REFERENCE address overlay (into adjacent storage), BY CONTENT sized copy, numeric length-mismatch leading-byte overlay"
  ],
  "case_id": "GNURUST.CALL.LAYOUT.ATLAS.1",
  "crate": "gnucobol-rs",
  "crate_version": "0.7.47",
  "damage_if_overclaimed": "assuming CALL parameters are length-checked/truncated hides the real hazard -- a LINKAGE length that disagrees with the caller silently overlays adjacent storage (read and write); trusting a BY CONTENT over-read value reads uninitialized memory",
  "hash_chain": {
    "inputs_sha256": "67d959c71b2a125a21394ac28d8cac947bff1a0fde577c9be627c03ae856f3c9"
  },
  "inputs": {
    "claim_ladder_entry_sha256": "7e8d7fb0cd27994d959d2ed3ddef173cb8b96b7112cc83e5673d89790e19d214",
    "receipt_sha256": "7d4436fb710d6bf6cba0bc8e99ebcfe2b2740b0558cf041a7ccd0fc2afe10ccb"
  },
  "kind": "court-casefile",
  "lie_prevented": [
    "a CALL parameter is length-checked and a mismatch is truncated or padded -- NO: BY REFERENCE is a raw ADDRESS OVERLAY with NO bounds, so a callee item longer than the caller's field silently reads (and a write corrupts) ADJACENT caller storage -- a mismatched-length copybook does not error, it reads the neighboring field; BY CONTENT instead copies (caller safe) and an over-read past that copy is UNDEFINED, not space-padded"
  ],
  "negative_capability_ids": [
    "NEG.SUBPROGRAM-EXECUTION-GNUCOBOL-RS-RUNS-NO",
    "NEG.THE-VALUE-OF-A-BY-CONTENT-OVER-READ-PAST",
    "NEG.BY-VALUE-BYTE-LAYOUT",
    "NEG.OCCURS-DEPENDING-ON-ACROSS-THE-LINKAGE-B",
    "NEG.OPTIONAL-OMITTED-PARAMETERS",
    "NEG.THE-RETURNING-PHRASE",
    "NEG.ALL-DIALECTS"
  ],
  "negative_claims": [
    "subprogram execution (gnucobol-rs runs no subprograms -- multi-module runtime is L8)",
    "the value of a BY CONTENT over-read past the copy (undefined / uninitialized memory)",
    "BY VALUE byte layout",
    "OCCURS DEPENDING ON across the linkage boundary",
    "OPTIONAL / OMITTED parameters",
    "the RETURNING phrase",
    "all dialects",
    "lie prevented: a CALL parameter is length-checked and a mismatch is truncated or padded -- NO: BY REFERENCE is a raw ADDRESS OVERLAY with NO bounds, so a callee item longer than the caller's field silently reads (and a write corrupts) ADJACENT caller storage -- a mismatched-length copybook does not error, it reads the neighboring field; BY CONTENT instead copies (caller safe) and an over-read past that copy is UNDEFINED, not space-padded"
  ],
  "oracle": {
    "detail": "cobc CALL USING + LINKAGE parameter layout (libcob/call.c address passing)",
    "dialect_profile_id": "gnucobol-3.2.0-default",
    "dialect_profile_sha256": "a321d7dfd4db4b492c1dfdaddc16eb876cf204d82630b8a54e5f6e01ab00c77c",
    "oracle_kind": "gnucobol-3.2-admitted",
    "upstream_court": "GNURUST.CALL.LAYOUT.ATLAS.1"
  },
  "positive_claims": [
    "observed the BYTE-EXACT CALL USING parameter layout and length-mismatch behavior under the gnucobol-3.2.0-default witness, verified by the sweep (5/0), deepening GNURUST.CALL.EXTENSION.ATLAS.1: BY REFERENCE is a pure ADDRESS OVERLAY so a callee LINKAGE item LARGER than the caller's field reads PAST it into ADJACENT caller storage (X(5) over X(3) 'ABC' beside 'XYZ' -> the callee sees 'ABCXY', no truncation/padding/bounds) and the callee's write lands back in the caller's bytes ('ABC' -> 'ZBC')",
    "BY CONTENT passes a SIZED COPY leaving the caller UNCHANGED ('DEF')",
    "and a numeric LINKAGE 9(2) over a caller 9(4)=1234 overlays the LEADING display bytes positionally ('12')"
  ],
  "replay": {
    "command": "bash lab/oracle/call_layout_atlas_sweep.sh",
    "exit_code": 0
  },
  "results": {
    "fail": 0,
    "pass": 5,
    "total": 5,
    "verdict": "pass"
  },
  "schema": "kobold-forensic-casefile-v1"
}