<!-- DO NOT EDIT BY HAND. Generated from casefile.json by kobold-courts.
     Evidence of record: casefile.json. Portable attestations: sarif.json, intoto-statement.json, dsse-envelope.json. -->
# Forensic case file — GNURUST.PUBLIC.CORPUS.1 (court-casefile)

**Verdict: PASS** · lab/corpus/run.py 25 corpora -> reports/public-corpus-index.json · crate `gnucobol-rs` meta index (gnucobol-rs lab)

- **Oracle:** none -- a reference/meta court (like GNURUST.COVERAGE.1)
- **Byte domain(s):** a curated index of public corpora + metadata (no bytes admitted)
- **Replay:** `none -- a reference/meta court (like GNURUST.COVERAGE.1)`
- **Authority:** STATUS.md · receipt_status: no-trust2-receipt

## Positive claims (2)
- an admitted INDEX of 25 public COBOL/GnuCOBOL corpora (17 GnuCOBOL-runnable, 5 mainframe-realistic, 3 research-mining) as a HOSTILE WITNESS SET for gap discovery -- real public terrain to grow the court map against. Records per corpus: source URL, tier, declared features, note, status
- plus a best-first-10 ingestion order, the future companion campaigns (GNURUST.PUBLIC.GAP.1 run+classify, GNURUST.BUILD.PROFILE.1 ABI/config binding, GNURUST.INDEXED.FILE.ATLAS.1), and a behavioral-parity ladder (Level 0 static layout .. Level 8 multi-module runtime parity) locating the current frontier at Level 6 (bounded trace replay via the IF/EVALUATE/PERFORM/TABLE/FILE-FLOW slices). INDEX ONLY

## Negative claims (6) — negative capability is the trust surface
- parity over any corpus
- that any corpus was fetched/compiled/run
- the recorded commit hashes or licenses (unverified at index time)
- that the declared feature flags were verified by a code scan
- exhaustiveness of the public-COBOL universe
- lie prevented: we tested 25 public COBOL datasets -- NO: this court admits the SOURCES for gap discovery, fetches/compiles/runs NOTHING, and claims NO parity; the running + failure-classification is the future GNURUST.PUBLIC.GAP.1, and behavioral parity is reached by court admission up the ladder, never by a speculative interpreter

## Damage if overclaimed
presenting a corpus index as parity/test coverage fabricates evidence the project has not produced -- the exact overclaim the method exists to prevent

> Generated forensic evidence (TRUST.4). The binding record is `casefile.json`; this `.md` is a rendering.
> Portable attestations: `sarif.json` (findings), `intoto-statement.json` (provenance), `dsse-envelope.json`.
