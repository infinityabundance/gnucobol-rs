{
  "authority": {
    "current_authority": "STATUS.md",
    "generated_by": "kobold-courts",
    "receipt_status": "current"
  },
  "byte_domains": [
    "compiler-directive byte delta from the default profile: -fbinary-size (layout), -fbinary-byteorder (endianness), -fbinary-truncate (MOVE result)"
  ],
  "case_id": "GNURUST.DIRECTIVE.VARIANCE.ATLAS.1",
  "crate": "gnucobol-rs",
  "crate_version": "0.7.61",
  "damage_if_overclaimed": "decoding a record produced under a non-default binary-size/byteorder/truncate with the default profile silently mis-offsets fields, reverses binary values, or mis-rounds MOVEs; assuming the profile can be auto-detected from bytes invents a build environment that was never recorded",
  "hash_chain": {
    "inputs_sha256": "ac7c8d5c16df1cd95840d5b8ccf1eeeb6ddc0e50d95bc6cf18338a22d10821b2"
  },
  "inputs": {
    "claim_ladder_entry_sha256": "da3d04906bc9ae01c86fe78288220d0d3d2fd6928b57e174bea92af730a59af3",
    "receipt_sha256": "d80c1cfb28c2ad0a1ca484540fdc7d3bfe9736de2ea26137fc792303fcecec53"
  },
  "kind": "court-casefile",
  "lie_prevented": [
    "a COBOL binary's record bytes are fixed by the copybook alone -- NO: a COMPILER DIRECTIVE shifts them: -fbinary-size=2-4-8 makes a small COMP field 2 bytes (mis-offsetting every later field), -fbinary-byteorder=native flips multi-byte COMP endianness, and -fno-binary-truncate changes what a MOVE stores -- so a correct decode REQUIRES the producer's build profile (BUILD.PROFILE.1), it cannot be inferred from the copybook"
  ],
  "negative_capability_ids": [
    "NEG.IMPLEMENTATION-OF-NON-DEFAULT-DIRECTIVES",
    "NEG.AUTO-DETECTION-OF-A-BINARY-S-BUILD-PROFI",
    "NEG.A-COMPLETE-ENUMERATION-OF-EVERY-COBC-DIR",
    "NEG.DIALECT-SELECTION-FLAGS-STD-OWNED-BY-GNU",
    "NEG.CODE-GENERATION-OPTIMIZATION-DIRECTIVES",
    "NEG.RUNTIME-ENVIRONMENT-VARIABLES-COB",
    "NEG.VENDOR-SPECIFIC-DIRECTIVES"
  ],
  "negative_claims": [
    "implementation of non-default directives (gnucobol-rs decodes under the BUILD.PROFILE.1 default only)",
    "auto-detection of a binary's build profile from its bytes",
    "a complete enumeration of every cobc directive",
    "dialect-selection flags (-std, owned by GNURUST.DIALECT.RUNTIME.ATLAS.1)",
    "code-generation/optimization directives",
    "runtime environment variables (COB_*)",
    "vendor-specific directives",
    "lie prevented: a COBOL binary's record bytes are fixed by the copybook alone -- NO: a COMPILER DIRECTIVE shifts them: -fbinary-size=2-4-8 makes a small COMP field 2 bytes (mis-offsetting every later field), -fbinary-byteorder=native flips multi-byte COMP endianness, and -fno-binary-truncate changes what a MOVE stores -- so a correct decode REQUIRES the producer's build profile (BUILD.PROFILE.1), it cannot be inferred from the copybook"
  ],
  "oracle": {
    "detail": "cobc -f<directive> compile + run (cobc/config.c directive engine + libcob/move.c binary codec)",
    "dialect_profile_id": "gnucobol-3.2.0-default",
    "dialect_profile_sha256": "a321d7dfd4db4b492c1dfdaddc16eb876cf204d82630b8a54e5f6e01ab00c77c",
    "oracle_kind": "gnucobol-3.2-admitted",
    "upstream_court": "GNURUST.DIRECTIVE.VARIANCE.ATLAS.1"
  },
  "positive_claims": [
    "observed how COMPILER DIRECTIVES change the record bytes under the gnucobol-3.2.0 witness, verified by the sweep (6/0): -fbinary-size=2-4-8 allocates 9(2) COMP as 2 bytes instead of 1 so a record's COMP offsets and total length SHIFT (group LENGTH 7 -> 8)",
    "-fbinary-byteorder=native stores a COMP field host-little-endian (4660 -> 34 12) vs the default config big-endian (12 34)",
    "and -fbinary-truncate (default, ANSI) truncates a COMP MOVE to the PIC digits (MOVE 300 TO 9(2)COMP -> 00) while -fno-binary-truncate keeps the raw binary value (-> 44) -- the byte-level delta from the BUILD.PROFILE.1 default profile"
  ],
  "replay": {
    "command": "bash lab/oracle/directive_variance_atlas_sweep.sh",
    "exit_code": 0
  },
  "results": {
    "fail": 0,
    "pass": 6,
    "total": 6,
    "verdict": "pass"
  },
  "schema": "kobold-forensic-casefile-v1"
}