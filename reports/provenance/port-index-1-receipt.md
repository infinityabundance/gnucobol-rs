<!-- generated by `cargo run -p gnucobol-rs-port-index -- parity` — do not edit by hand;
     freshness-gated by `gnucobol-rs-port-index check` (the scoreboard is regenerated from the
     live C↔Rust symbol join, so it can never go stale). -->

# PORT-INDEX.1 — typed C↔Rust symbol parity (+ PORT-INDEX.1a false-closure repair)

**Tool:** `crates/gnucobol-rs-port-index` · **Outputs:** `LIBCOB-PARITY.md`,
`reports/libcob-parity.json`, `reports/port-index/parity-detailed.json`.

## What this seals

PORT-INDEX.1 replaced grep / name-presence parity with **typed C↔Rust symbol parity**. It indexes
the 1170 admitted `libcob` source functions, classifies each by **preprocessor status**
(1169 compiled into the admitted oracle; the rest `#if 0` / config-gated), and joins them
against a **real-`fn`** index of the Rust port that is comment- and string-aware. A libcob function
counts as ported **only when a real Rust `fn` of that name exists** — a name appearing solely in a
doc comment is a `doc_only` false hit, never a port. PORT-INDEX.1a then closed every false hit it
exposed in the completed files (`numeric.c` 13, `move.c` 10, `cobgetopt.c` 1) by giving each faithful
Rust counterpart its **exact C name** (or adding the named wrapper).

**Live total:** 313 / 1169 compiled libcob functions have a real Rust `fn` (856 gap).

## Completed files (100% active compiled-parity)

| libcob file | source | compiled | active | inactive | doc-only | missing | active parity |
|---|---:|---:|---:|---:|---:|---:|---:|
| `numeric.c` | 105 | 104 | 99 | 5 | 1 | 0 | 100.0% |
| `move.c` | 57 | 57 | 56 | 1 | 0 | 0 | 100.0% |
| `strings.c` | 34 | 34 | 34 | 0 | 0 | 0 | 100.0% |
| `cconv.c` | 9 | 9 | 9 | 0 | 0 | 0 | 100.0% |
| `termio.c` | 18 | 18 | 18 | 0 | 0 | 0 | 100.0% |
| `cobgetopt.c` | 4 | 4 | 4 | 0 | 0 | 0 | 100.0% |

## In-progress files (partial — incremental subsystem ports)

| libcob file | source | compiled | active | gap | active parity |
|---|---:|---:|---:|---:|---:|
| `intrinsic.c` | 238 | 238 | 44 | 193 | 18.5% |
| `mlio.c` | 47 | 47 | 8 | 35 | 17.0% |
| `reportio.c` | 39 | 39 | 18 | 21 | 46.2% |

## Gates

`gnucobol-rs-port-index check` (anti-staleness: committed map == fresh re-derivation, and this
receipt's scoreboard == live) runs in both `lab/verify-sealed-courts.sh` and the docs-staleness
gate `lab/check-docs.sh`. It is complemented by the **C-vs-Rust doxygen** coverage gate
(`DOXYGEN-PARITY.md`, authoritative for the *compiled* C set — both the C and the native-Rust
doxygen are regenerated automatically so nothing needing a port slips through).

## Not claimed

Active parity is **symbol** parity — a real Rust `fn` exists — not behaviour parity (the oracle
sweeps) and not yet evidence-mapped. A future `function-evidence` milestone will add an
*evidenced* column linking each active function to its court / sweep / test receipt.

