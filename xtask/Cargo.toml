[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
publish = false
description = "gnucobol-rs governance tooling (Rust; replaces the lab/*.py scripts). Not published."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
